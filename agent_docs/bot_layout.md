# bot/ Package Layout (Sprint 9 modularization CLOSED)

**Bit 9.3-iii.c (2026-05-11) DELETED `bot/_impl.py` entirely.** Sprint 9 main
modularization is CLOSED. The original ~18,000-LOC `bot/_impl.py` no longer
exists; every class and function lives in its canonical submodule home, and
the package surface is the set of canonical submodules listed in the "Project
file map" section below.

**Historical trail (most recent first):**
- Bit 10.3 (2026-05-12) — `bot/ai/` subpackage created. 3 files relocated
  from repo root to `bot/ai/`: analyst.py + auditor.py + researcher.py
  (~4,114 LOC). 2 `__file__`-derived path sites (auditor.py `SCRIPT_DIR`
  anchoring state.db / auditor_state.db / .env / scan_journal.jsonl /
  opportunity_journal.jsonl; researcher.py `SCRIPT_DIR` anchoring state.db /
  auditor_state.db / researcher_state.db / .env) extended to a 3-level
  dirname chain (`Path(__file__).resolve().parent.parent.parent`) so they
  re-anchor to repo root from `bot/ai/`. analyst.py uses CWD-relative
  `state.db` literal — VPS cron invokes from repo root, behavior
  unchanged. Zero Python `import analyst|auditor|researcher` sites pre-
  or post-move (these are standalone CLI entrypoints, not modules). No
  `.importlinter` carve-out (no `bot/helpers/*.py` references them); net
  contracts unchanged at 5. VPS crontab retarget (`python3 bot/ai/X.py`)
  is operator concern, filed as followup ticket.
- Bit 10.4 (2026-05-12) — `bot/snapshots/` subpackage created. 4 files
  relocated from repo root to `bot/snapshots/`: dashboard_snapshot.py +
  bot_state_snapshot.py + market_observations_snapshotter.py + supabase_sync.py.
  3 `__file__`-derived path sites (dist_config.json, KILL_SWITCH_FILE,
  state.db) anchored to repo root via the 3-level dirname chain (matches
  bot/engines/weather_engine.py:806-807). `.importlinter` helpers-leaf
  forbidden_modules extended with `bot.snapshots`; net contracts stays at 5.
  (Sprint 14-A Bit 3 update, 2026-05-17, ticket 86b9zfbt8: the
  dist_config.json data artifact relocated to `ops/runtime/dist_config.json`;
  the 3-level anchor still resolves to repo root, but the join in
  `dashboard_snapshot.py` appends `ops/runtime/` to find the file.)
- Bit 9.3-iii.c (`TBD`, 2026-05-11) — DELETE bot/_impl.py + kill-switch
  runtime-freshness fix bundled (scanner/executor reads of WEATHER/HOURLY/
  BRACKET kill-switch flags converted to `bot.constants.X` module-attribute
  access). NEW `bot/runtime_config.py` PEP 562 dual-probe replaces bot._impl
  as the getattr target for `bot/snapshots/dashboard_snapshot.py` + `bot/snapshots/supabase_sync.py` (post Bit 10.4 sibling-reorg).
- Bit 9.3-iii.b (`ba46d77`, 2026-05-11) — `_BotProxy` retirement; bot/__init__.py
  is now docstring-only.
- Bit 9.3-iii.a (`ec88540`, 2026-05-11) — 4 boot-time bindings +
  cal_mlp warmup log → NEW `bot/boot.py` clean leaf.
- Bit 9.3-ii — bot/__main__.py swap to direct `from bot.main_loop import
  MainLoop`; orphan-DB Layer-3 watchdog → `bot/orphan_db_watchdog.py`.
- Bit 9.3.5 — OrderFlowEngine + KalshiOrderFlowTracker → `bot/order_flow.py`.
- Bit 9.3-i — MainLoop → `bot/main_loop.py`.
- Bit 9.2 — SettlementTracker + discover_active_windows → `bot/settlement.py`.
- Bit 9.1 — OrderExecutor → `bot/executor.py`.
- Bit 8.1 — OpportunityScanner → `bot/scanner/__init__.py`.
- Bit 7.1 — StateManager → `bot/state.py`.
- Bits 6.1-6.3 — VolatilityEngine/ProbabilityEngine/CalibrationEngine → `bot/engines/`.
- Bits 4.1-4.5b — Logger/TelegramNotifier/KalshiClient/Fetchers/Feeds.
- Bit 3.2 — helpers → `bot/helpers/`.
- Bit 3.1 — module-level constants → `bot/constants.py`.

The body of the original bot/_impl.py is GONE. The line ranges below were
auto-verifiable against bot/_impl.py pre-deletion; post-Bit-9.3-iii.c they
are HISTORICAL ONLY (kept as a breadcrumb of the extraction trail). Repo
modularization plan (`kb/decisions/repo-modularization-plan-may05.md`)
has CLOSED at this milestone.

## Regenerate

This doc no longer auto-regenerates from bot/_impl.py (the file is gone).
The regression test `test_bot_layout_class_lines_match_bot_impl` self-skips
when bot/_impl.py is absent (parses an empty class set, matches the
empty-class table below). Manual updates to the file map below are required
when modules move OR get added under bot/. Regenerate the line-range scan
(historical only) with:

```bash
python3 -c "
import re
text = open('bot/_impl.py').read(); lines = text.splitlines(); total = len(lines)
classes = []
for i, line in enumerate(lines, 1):
    m = re.match(r'^class ([A-Za-z_][A-Za-z0-9_]*)', line)
    if m: classes.append((i, m.group(1)))
print(f'Total bot/_impl.py lines: {total}')
print('| Lines | Class | Size |'); print('|---|---|---|')
for idx, (start, name) in enumerate(classes):
    end = classes[idx+1][0]-1 if idx+1 < len(classes) else total
    print(f'| {start}–{end} | \`{name}\` | {end-start+1} |')
"
```

## Public API surface (separate concern)

The class line ranges below describe `bot/_impl.py`'s **internal layout**.
For the **public API contract** of the `bot` package, the source of truth is
`tests/contracts/public_api.json` (Pillar 1 of the testing-foundation-sprint,
ticket 86b9ve0xt). Regenerate with `make api-snapshot-regen` after
intentional surface changes; CI gates on zero-diff via
`tests/contracts/test_public_api_snapshot.py`.

The snapshot has three layers:

1. **Static walk of public submodules under `bot.*`** — every public name
   in `bot.engines.*`, `bot.feeds.*`, `bot.fetchers.*`, `bot.helpers.*`,
   `bot.constants`, `bot.kalshi_client`, `bot.logger`, `bot.notifier`,
   etc., with full signatures (parameters + returns + class methods).
   `bot._impl` and `bot._thread_env` are excluded from this walk — their
   contents are covered by Layers 2 and 3.

2. **Static walk of locally-defined public classes inside `bot/_impl.py`** —
   post-Bit-9.3.5 the resident class list is **empty** (verify with the
   class table below). `OrderExecutor` moved to `bot.executor` in Bit 9.1;
   `SettlementTracker` moved to `bot.settlement` in Bit 9.2; `MainLoop`
   moved to `bot.main_loop` in Bit 9.3; `OrderFlowEngine` +
   `KalshiOrderFlowTracker` moved to `bot.order_flow` in Bit 9.3.5.
   The list is auto-derived, not hardcoded, so extractions naturally
   move classes out of this layer and into Layer 1 (`OpportunityScanner` →
   Bit 8.1; `StateManager` → Bit 7.1; `CalibrationEngine` → Bit 6.3;
   `ProbabilityEngine` → Bit 6.2; `VolatilityEngine` → Bit 6.1; etc.).
   Full class signatures.

3. **Runtime proxy probe** — RETIRED in Bit 9.3-iii.b (2026-05-11).
   Pre-retirement: imported `bot` at runtime, enumerated every public
   name accessible via `getattr(bot, name)` (the `_BotProxy` forwarded
   attribute access to `bot._impl`, including `from bot.config import *`
   (Bit 12.1 retarget; pre-Bit-12.1 was `from config import *`) and
   `from bot.constants import *` resolutions). Post-retirement the
   proxy is gone — `getattr(bot, name)` no longer falls through. Layer 1
   (griffe static walk of bot.* submodules) covers every name's canonical
   home; the probe is now a no-op and `__bot_proxy_attrs__` is absent
   from regenerated snapshots.

This layout doc and the snapshot are orthogonal: this doc tracks *where*
code lives in `bot/_impl.py` (line ranges shift on every extraction);
`public_api.json` tracks *what* the public surface is (the 3-layer
structure keeps it stable across extractions).

## Top-level structure (approximate)

bot/_impl.py interleaves imports, residual helpers, classes, and a tail
entry point. Boundaries below are inexact (no AST split exists yet);
verify by reading 5–10 lines around each line number before quoting.

| Lines | Section |
|---|---|
| 1–~110 | Header import block (`bot._thread_env` imports BEFORE `numpy` — load-bearing per CLAUDE.md; `scripts/cal_mlp/` is also added to sys.path here for the bare `from integration import` calls later in the file). `from bot.constants import *` (Bit 3.1) at ~83; `from bot.helpers import *` + explicit underscore re-exports for `bot.helpers.validators` and `bot.helpers.breakers` (Bit 3.2) immediately after. `from bot.logger import Logger` (Bit 4.1), `from bot.notifier import TelegramNotifier` (Bit 4.2), `from bot.kalshi_client import KalshiClient` (Bit 4.3), `from bot.fetchers import DeribitDVOLFetcher, CoinGlassFetcher` (Bit 4.4), and `from bot.feeds import CoinbaseFeed, CrossExchangeFeed, KalshiFeed, OrderbookSchemaError` (Bit 4.5a + 4.5b) follow at ~100–~104. Verify with `grep -n "^from bot\." bot/_impl.py`. |
| (deleted) | Bit 9.3-iii.c (2026-05-11) DELETED bot/_impl.py entirely. Pre-deletion residual content was 591 LOC (16 class/function re-exports + 2 module aliases + breadcrumb comments + 23-line residual-shim docstring). The 4 boot-time bindings (`_HPSB_VALIDATOR_UNAVAILABLE_REASON`, `_HPSB_MISSING_BLEEDERS = _validate_high_price_stc_block_bleeder_strings()`, `_BLEED_BLOCK_MISSING_BLEEDERS = _validate_bleed_block_bleeder_strings()`, `compute_for_15m_main_path = make_compute_for_15m_main_path()`) + cal_mlp warmup boot log relocated to clean-leaf `bot/boot.py` (Bit 9.3-iii.a). The orphan-DB watchdog Layer-3 helpers were already relocated to `bot/orphan_db_watchdog.py` (Bit 9.3-ii). `_swallow_persist_exception` was relocated to `bot/feeds/coinbase.py` in Bit 4.5a alongside its sole consumer; D2.3 (2026-05-17, ticket `86b9zkppt`) DELETED the helper when CoinbaseFeed stopped routing persist through `asyncio.to_thread` (the dedicated sampler daemon thread runs persist directly under its own try/except). **Bit 6.3 path-B (2026-05-10) relocated** `_CALIBRATION_ENGINE` singleton + `_CAL_REGISTRY` dict + `_derive_subtype` + `_derive_asset_filter` + `_resolve_cal_engine` to `bot/engines/calibration.py`; bot/_impl.py reaches them via the `from bot.engines import calibration as _cal_state` alias near the top of the file. **Bit 8.1 path-A++ (2026-05-10) relocated** the `_TELEGRAM` module-level singleton to `bot/notifier.py` alongside its `TelegramNotifier` class; bot/_impl.py reaches it via `import bot.notifier as _telegram_state` plus `_telegram_state._TELEGRAM` module-attribute access (mirrors the `_cal_state` pattern; preserves mutation freshness across the five consumers post-Bit-9.3 — bot/_impl.py for the `_alert_orphan_db_holder` orphan-DB Layer-3 helper at bot/_impl.py:431 + bot/main_loop.py for MainLoop reads + WRITE + bot/scanner/__init__.py + bot/executor.py + bot/settlement.py). **Bit 9.2 (2026-05-10) RETIRED** the `_append_raw_api_journal` L81 alias-import — both historical SettlementTracker callers moved to `bot/settlement.py` with the class and now use the public-name `append_raw_api_journal` directly. **Bit 9.3 (2026-05-10) atomic narrative flip**: the bot/_impl.py slot in the `_telegram_state._TELEGRAM` consumer list changed semantics — was "MainLoop reads only" (the Bit-9.2-era frame); is now "for the `_alert_orphan_db_holder` orphan-DB helper" (since MainLoop reads MOVED with the class extraction to bot/main_loop.py). |
| (formerly ~660–~767, no longer exists post-Bit-9.3-ii) | Class section was **empty** by Bit 9.3.5 — only breadcrumbs remained. Sprint-4 through Sprint-9 extracted classes are documented via `# <class> → bot/<dest>.py (Bit X.Y)` one-liners now living inside the `~125–~591` residual range above. `discover_active_windows()` shipped to `bot/settlement.py` in Bit 9.2. |

### Class-body end vs class-range note

Post-Bit-9.3.5 there are no classes left in `bot/_impl.py` — the class
table below is empty. The chronic over-counts that historically came
from inter-class module-level defs are moot for the same reason.
Post-Bit-9.3.5 the file contains breadcrumbs for every Sprint-4 + Sprint-6 +
Sprint-7 + Sprint-8 + Sprint-9 leaf extracted so far —
CoinbaseFeed (Bit 4.5a), KalshiFeed (Bit 4.5b), DeribitDVOLFetcher (Bit 4.4),
CoinGlassFetcher (Bit 4.4), CrossExchangeFeed (Bit 4.5a),
VolatilityEngine (Bit 6.1), ProbabilityEngine (Bit 6.2),
CalibrationEngine (Bit 6.3), StateManager (Bit 7.1),
OpportunityScanner (Bit 8.1), OrderExecutor (Bit 9.1),
SettlementTracker + discover_active_windows (Bit 9.2),
MainLoop (Bit 9.3), and OrderFlowEngine + KalshiOrderFlowTracker (Bit 9.3.5) —
all named with one-line `→ bot/<dest>` breadcrumbs.
Bit 4.5a/4.5b/6.1/6.2/6.3 deliberately inserted breadcrumb comments at
each cut site (`# CoinbaseFeed → bot/feeds/coinbase.py (Bit 4.5a, 2026-05-08).`,
`# KalshiFeed → bot/feeds/kalshi.py (Bit 4.5b, 2026-05-09).`,
`# OrderbookSchemaError → bot/feeds/orderbook_schema.py (Bit 4.5a, 2026-05-08).`,
`# CrossExchangeFeed → bot/feeds/cross_exchange.py (Bit 4.5a, 2026-05-08).`,
`# VolatilityEngine → bot/engines/volatility.py (Bit 6.1, 2026-05-09).`,
`# ProbabilityEngine → bot/engines/probability.py (Bit 6.2, 2026-05-09).`,
`# CalibrationEngine → bot/engines/calibration.py (Bit 6.3, 2026-05-10).`)
and `tests/contracts/test_feeds_extraction.py::test_class_not_defined_in_bot_impl`
+ `tests/contracts/test_engines_extraction.py::test_class_not_defined_in_bot_impl`
+ `tests/contracts/test_engines_extraction.py::test_probability_class_not_defined_in_bot_impl`
+ `tests/contracts/test_engines_extraction.py::test_calibration_class_not_defined_in_bot_impl`
enforce the negative contract for all seven extracted classes.

## Classes (auto-verifiable)

Originally generated 2026-05-10 from `grep -nE '^class ' bot/_impl.py`. LOC trajectory: at 9.3.5 ship ~767 → 9.3-ii ~582 → 9.3-iii.a ~565 → 9.3-iii.b 591. **Bit 9.3-iii.c (2026-05-11) DELETED bot/_impl.py** — the file no longer exists; the class table above is permanently empty.

| Lines | Class | Size |
|---|---|---|
| (none) | (post-Bit-9.3.5 — class section empty) | — |

(Post-Bit-9.3.5: OrderFlowEngine + KalshiOrderFlowTracker class bodies
extracted to `bot/order_flow.py`; the
`# OrderFlowEngine + KalshiOrderFlowTracker → bot/order_flow.py (Bit 9.3.5, 2026-05-10)`
breadcrumb at line ~660 records the deletion. Verify with
`grep -nE '^class ' bot/_impl.py` — should return nothing.)

## Project file map (root, 2026-05-05)

Live trading process:
- `bot/_impl.py` — **DELETED in Bit 9.3-iii.c (2026-05-11)**. Pre-deletion the residual re-export shim peaked at 591 LOC (9.3-iii.b ship state — 16 class/function re-exports + 2 module aliases + breadcrumb comments + 23-line residual-shim docstring; zero classes post-9.3.5). LOC trajectory: 9.3.5 ship ~767 → 9.3-ii ~582 → 9.3-iii.a ~565 → 9.3-iii.b 591 → DELETED. **Sprint 9 main modularization closes at Bit 9.3-iii.c** — the original ~18,000 LOC bot/_impl.py is gone, the canonical-submodule package (bot/main_loop.py, bot/scanner/__init__.py, bot/executor.py, bot/settlement.py, bot/state.py, bot/order_flow.py, bot/orphan_db_watchdog.py, bot/boot.py, bot/runtime_config.py + all earlier Sprint 3-8 extractions) IS the package.
- `bot/runtime_config.py` — **NEW in Bit 9.3-iii.c (2026-05-11)**. PEP 562 module-level `__getattr__` dual-probe of bot.constants → bot.config (Bit 12.1, 2026-05-12 retargeted the second probe leg from repo-root `config.py` → `bot/config.py`). Replaces bot._impl as the getattr target for `bot/snapshots/dashboard_snapshot.py` + `bot/snapshots/supabase_sync.py` (post Bit 10.4 sibling-reorg, 2026-05-12) runtime-config introspection. Preserves getattr-with-default semantics + mutation freshness for runtime-mutable kill-switch flags (the scanner's auto-kill writes target bot.constants.X; this module reads through to that same module attr on the next snapshot tick).

- `bot/order_flow.py` — `OrderFlowEngine` + `KalshiOrderFlowTracker` classes (orderbook flow signals — cross-exchange consensus + funding rate + Kalshi orderbook depth/velocity/spread/convergence; 378-line file: ~89-line header + ~122-line OFE class body + ~155-line KOFT class body + small spacers) extracted from `bot/_impl.py` (Bit 9.3.5, 2026-05-10). Strict-ban for numerical imports (zero numpy/scipy/torch/sklearn/pandas — pure stdlib + 21 explicit `bot.constants` names). Re-exported into `bot/_impl.py` via `from bot.order_flow import OrderFlowEngine, KalshiOrderFlowTracker` (line ~120, immediately after the Bit 9.3 MainLoop re-export). **Clean leaf — mirrors Bit 9.2 SettlementTracker shape but smaller surface**: bot/order_flow.py has zero references to names defined below the line-119 re-export point in bot/_impl.py; zero `_telegram_state` consumers; zero `_cal_state` consumers; net `.importlinter` contracts stays at 5 (just adds `bot.order_flow` to `helpers-leaf` `forbidden_modules`). The two `# REMOVE BIT 9.3.5` markers in MainLoop.__init__'s late-binding block (Bit 9.3 form) collapsed to a top-level `from bot.order_flow import OrderFlowEngine, KalshiOrderFlowTracker` in bot/main_loop.py. **Sister cleanup atomic in same commit**: bot/scanner/__init__.py `Optional["OrderFlowEngine"]` and `Optional["KalshiOrderFlowTracker"]` forward-refs UNQUOTED — bot/order_flow.py has zero bot.scanner edges, so the new top-level `from bot.order_flow import` in bot/scanner/__init__.py resolves cleanly at scanner load time. The 5-consumer `_telegram_state._TELEGRAM` enumeration is UNCHANGED — OFE+KOFT do not emit Telegram alerts. **Imports**: 21 explicit names from `bot.constants` (CROSS_EXCHANGE_CONSENSUS_MIN, FUNDING_RATE_ELEVATED/EXTREME, OFA_CONSENSUS_BOOST/REDUCE, OFA_ELEVATED/EXTREME_FUNDING_REDUCE, OFA_LEAD_BOOST, OFA_MAX_ADJUSTMENT, KALSHI_OFT_BUFFER_SIZE/DEPTH_DRAIN_PCT/IMBALANCE_STRONG/WEAK/LOG_INTERVAL/MIN_SNAPSHOTS/SHADOW_MODE/STALE_SECONDS, OFA_KALSHI_CONVERGENCE_BOOST/DEPTH_DRAIN_BOOST/IMBALANCE_BOOST/REDUCE) + stdlib (logging, time, collections.deque, typing.{Dict,List,Optional,Set,Tuple}). Sprint 9 closes here.

- `bot/main_loop.py` — `MainLoop` class (continuous observation loop; ~2,200-line file: ~150-line header + 1,999-line MainLoop class body + late-binding inserts + new top-level `from bot.order_flow import` post-Bit-9.3.5) extracted from `bot/_impl.py` (Bit 9.3, 2026-05-10). Strict-ban for numerical imports (zero numpy/scipy/torch/sklearn/pandas — orchestration only; engines carry the libs transitively via `models.py`). Re-exported into `bot/_impl.py` via `from bot.main_loop import MainLoop` (line ~119) so `bot/__main__.py`'s `from bot._impl import MainLoop` continues to resolve at 9.3-i (the swap to direct `from bot.main_loop import MainLoop` is deferred to Bit 9.3-ii per master plan two-step atomic discipline). **Path-A (METHOD-BODY late-binding for the residual bot._impl names)**: bot/main_loop.py has ZERO top-level `from bot._impl import` or `import bot._impl`. Top-level would partial-module ImportError because bot/_impl.py at line ~119 re-exports `from bot.main_loop import MainLoop` BEFORE bot/_impl.py reaches `_HPSB_MISSING_BLEEDERS` at line ~349. **Post-Bit-9.3-iii.a** (2026-05-11), the Bit 9.3 method-body late-binding block inside `MainLoop.__init__` is GONE — the HPSB pair (`_HPSB_MISSING_BLEEDERS`, `_HPSB_VALIDATOR_UNAVAILABLE_REASON`) relocated to clean-leaf `bot/boot.py` and bot/main_loop.py top-imports them. `MainLoop.startup` still method-body late-binds `detect_orphan_db_holders` from `bot.orphan_db_watchdog` (Bit 9.3-ii relocation, orthogonal to bot._impl). Bit 9.3.5 had previously collapsed the prior OFE+KOFT entries to a top-level `from bot.order_flow import OrderFlowEngine, KalshiOrderFlowTracker` (bot/order_flow.py is a clean leaf — no partial-module risk). NO new `.importlinter` carve-out — bot/main_loop.py has zero top-level bot._impl edge in the import graph; net contracts stays at 5 (mirrors Bit 9.2 clean leaf). **Cross-class coupling preserved**: `_telegram_state._TELEGRAM` (Bit 8.1 path-A++; ~11 reads + 1 WRITE in `__init__`); `_cal_state._CALIBRATION_ENGINE` / `._CAL_REGISTRY` / `._resolve_cal_engine` (Bit 6.3 path-B). **`_telegram_state._TELEGRAM` consumer enumeration grows from 4 to 5 post-Bit-9.3**: bot/_impl.py STAYS for `_alert_orphan_db_holder` orphan-DB Layer-3 helper at bot/_impl.py:431 (transitively called from `MainLoop.startup` via `detect_orphan_db_holders(DB_PATH)`); bot/main_loop.py ADDS for MainLoop reads + WRITE. **Imports**: 23 explicit names from `bot.constants` + 1 from `bot.config` (ASSETS) + 1 from `market_config` (MARKET_CONFIGS) + 4 from `models` (EGARCHEstimator, MincerZarnowitzTracker, PositionSizer, calculate_taker_fee) + 3 from `integration` (`_calmlp_predictors`, `start_post_hoc_processor as _calmlp_start_posthoc`, `stop_post_hoc_processor as _calmlp_drain_pool`) + leaf classes (KalshiClient, Logger, TelegramNotifier, StateManager) + engines (CalibrationEngine, ProbabilityEngine, VolatilityEngine) + feeds (CoinbaseFeed, CrossExchangeFeed, KalshiFeed) + fetchers (DeribitDVOLFetcher, CoinGlassFetcher) + Sprint-8/9 peers (OpportunityScanner, OrderExecutor, SettlementTracker, discover_active_windows, OrderFlowEngine, KalshiOrderFlowTracker — Bit 9.3.5) + 2 helper underscore-imports (`_extract_tick_error_location` from `bot.helpers.breakers`, `dollars_str_to_cents` from `bot.helpers.strings`) + alias modules (`import bot.notifier as _telegram_state`, `from bot.engines import calibration as _cal_state`) + stdlib (datetime, json, logging, math, os, signal, sys, threading, time, datetime.timezone, collections.deque, typing.Dict/List/Optional). Sister Bit 9.4 (root CLAUDE.md sacred-rule semantics rewrite — `bot/__main__.py` is the entrypoint shim — sacred boundary, no logic; logic lives in `bot/<subpackage>/<module>.py`) ships separately or in parallel. Sprint 9 Bit 9.3 ships here.

- `bot/settlement.py` — `SettlementTracker` class (incremental settlement poller; ~1,275-line file: ~100-line header + 1,088-line class body + ~80-line `discover_active_windows()` module-level function) extracted from `bot/_impl.py` (Bit 9.2, 2026-05-10). Strict-ban for numerical imports (zero numpy/scipy/torch/sklearn/pandas — pure stdlib + sqlite3 + requests-via-KalshiClient). Re-exported into `bot/_impl.py` via `from bot.settlement import SettlementTracker, discover_active_windows` so MainLoop construction (search anchor: `self.tracker = SettlementTracker(`) + the single MainLoop call site (search anchor: `discover_active_windows(self.client)`) resolve via bot._impl namespace. **Path-A++ deviations** (NOT byte-for-byte): the 2 SettlementTracker call sites that previously used the L81-aliased underscore-prefix name now use the public `append_raw_api_journal` directly (matches bot/executor.py:98); the L81 alias-import in bot/_impl.py:285 RETIRED atomically with this Bit (zero callers remain). **Bundled with class** (per USER decision via AskUserQuestion deviating from master plan Phase AA): `discover_active_windows()` ships here even though master plan originally placed it with MainLoop in Bit 9.3 — settlement-adjacent in source layout, KalshiClient is already needed for SettlementTracker, and only true caller is MainLoop._refresh_active_windows. **Imports**: 8 explicit names from `bot.constants` (HOURLY_OBSERVATION_ENABLED, HOURLY_SERIES_TICKERS, LOG_RAW_IOC_FILLS, LOG_RAW_SETTLEMENTS, SERIES_TICKERS, SETTLEMENT_CHECK_SECONDS, TM_SWEEP_SHADOW_ENABLED, WEATHER_MIN_ENTRY_PRICE) + `append_raw_api_journal` from `bot.helpers.raw_api_journal` (public name) + 2 from `bot.helpers.strings` (dollars_str_to_cents, fp_str_to_int) + `KalshiClient` (type ann on `__init__` + discover_active_windows param) + `StateManager` (type ann) + `Logger` (type ann) + `from bot.engines import calibration as _cal_state` (Bit 6.3 path-B alias used at 4 sites for `_cal_state._CALIBRATION_ENGINE` + `_cal_state._resolve_cal_engine` reads) + `import bot.notifier as _telegram_state` (Bit 8.1 path-A++ alias for 4 `_telegram_state._TELEGRAM` reads) + `get_market_config` from `market_config` + 3 from `models` (calculate_fee, calculate_maker_fee, calculate_taker_fee) + stdlib (datetime, json, logging, re, sqlite3, threading, time, datetime.timezone, typing). **No `.importlinter` carve-out needed** — SettlementTracker has zero references to names defined below the line-117 re-export point in bot/_impl.py; clean leaf extraction; net contracts stays at 5. **Sister cleanup atomic in same commit**: bot/notifier.py docstring 3→4 consumer enumeration (adds bot/settlement.py); bot/__init__.py docstring extended; bot/CLAUDE.md Deploy step 3 catalog gains SettlementTracker paragraph; tests quadruple-walk extension across test_state_extraction.py (BOT_PY + SCANNER_PY + EXECUTOR_PY + SETTLEMENT_PY); test_order_outcome_vocab.py SCANNED_PATHS extended; test_tracker_tick_threaded.py BOT_PY → SETTLEMENT_PY retarget; test_low_price_shadow.py + test_stacking.py + test_tm_sweep_shadow.py + test_regression.py _read_bot()/_paths helpers extended to concat bot/settlement.py source; tests/integration/test_executor_extraction.py L81 positive pin flipped to negative. **Bundled bug fix** (ticket 86b9vppn3): pre-existing UnboundLocalError 'best_ask' in OpportunityScanner.scan() low_probability_15m insert_rejection branch (predates Bit 8.1 per git blame) — initialize best_ask=None at iteration start. Sprint 9 Bit 9.2 ships here; Bit 9.3 (MainLoop → bot/main_loop.py) is the last Sprint 9 leaf.

- `bot/executor.py` — `OrderExecutor` class (maker-first executor with adaptive taker escalation; ~5,413-line file containing the 5,284-line class body + ~129-line header) extracted from `bot/_impl.py` (Bit 9.1, 2026-05-10). Strict-ban for torch/sklearn/pandas (zero direct numerical imports; numpy/scipy reach transitively through `models.PositionSizer`/etc.). Re-exported into `bot/_impl.py` via `from bot.executor import OrderExecutor` so MainLoop construction (`self.executor = OrderExecutor(self.client, self.state, self.logger, main_loop=self, kalshi_feed=self.kalshi_feed)`) + test-suite instantiation sites resolve. **Post-Bit-9.3-iii.b (2026-05-11)** tests reach `OrderExecutor` via `from bot.executor import OrderExecutor` directly; pre-retirement the same callers used `bot.OrderExecutor(...)` via the `_BotProxy` chain (now retired). **Path-A++ deviations** (NOT byte-for-byte): (1) `_append_raw_api_journal` relocated to `bot/helpers/raw_api_journal.py` (1 OE caller + 2 ST callers); (2) 4 latent `OpportunityScanner._best_ask_depth(...)` AttributeError sites in OrderExecutor body fixed (closes ticket 86b9vn9r5; `_best_ask_depth` is a staticmethod on OrderExecutor itself). **Cycle break**: bot.executor imports `import bot.notifier as _telegram_state` (19 `_telegram_state._TELEGRAM` reads) but does NOT top-level-import bot.scanner; instead, a `_get_opportunity_scanner()` method-body helper handles the 7 OpportunityScanner staticmethod call sites (`_convert_orderbook_fp` × 2, `_best_yes_ask_cents` × 5). The asymmetry keeps bot/scanner/__init__.py's top-level `from bot.executor import OrderExecutor` clean. **Imports**: 97 explicit names from `bot.constants` + 4 from `bot.helpers.*` (dollars_str_to_cents, fp_str_to_int, tm_compute_contracts, tm_sweep_extract_depths) + `append_raw_api_journal` from `bot.helpers.raw_api_journal` (path-A++) + `KalshiClient` (type ann) + `StateManager` (type ann) + `Logger` (type ann) + `ProbabilityEngine` from `bot.engines.probability` + `get_market_config` from `market_config` + 3 from `models` (calculate_fee, calculate_taker_fee, strategy_to_group) + stdlib (datetime, heapq, json, logging, math, time, uuid, collections.deque, datetime.timezone, typing.Dict/List/Optional/Tuple). **Sister cleanup atomic in same commit**: bot/scanner/__init__.py drops `_get_order_executor()` helper + replaces 34 call sites with `OrderExecutor.X(...)` direct; `.importlinter` drops `scanner-no-impl-toplevel` block; tests/contracts/test_import_linter_contracts.py drops 3 peer-pin tests + EXPECTED_CONTRACTS entry; tests/contracts/public_api.json regenerated. Sprint 9 Bit 9.1 ships here; Bit 9.2 (SettlementTracker → bot/settlement.py) and Bit 9.3 (MainLoop → bot/main_loop.py) follow.
- `bot/constants.py` — module-level UPPER_SNAKE constants extracted from `bot/_impl.py` (Bit 3.1, 1,722-line, 484 constants). Re-exported into `bot/_impl.py` via `from bot.constants import *` near top of file.
- `bot/helpers/` — feature/sizing/cell-block helpers extracted from `bot/_impl.py` (Bit 3.2, ~790-line, 25 functions across 9 submodules: `time_features`, `derived_features`, `tm_sweep`, `sizing`, `cell_blocks`, `strings`, `strategy`, `validators`, `breakers`). Re-exported into `bot/_impl.py` via `from bot.helpers import *` plus explicit underscore re-exports for `validators` (3) and `breakers` (5) — star-import skips underscored names. Subsequent additions: `raw_api_journal` (Bit 9.1 path-A++ relocation), `orderbook` + `cohort_attribution` + `cohort_alerts` (Money Printer Roadmap P1.x), `band_calibration` (P4.1 2026-05-17), and `config_snapshot` (ticket 86b9zkp8p, 2026-05-17 — per-decision config-hash helper; `compute_config_snapshot()` + `persist_config_snapshot(conn)`; helper-leaf module reading `bot/constants.py` + `bot/config.py` + `market_config.py` as file bytes via sha256 to keep the leaf rule satisfied — see `bot/CLAUDE.md` "config_snapshot_id schema chain").
- `bot/logger.py` — `Logger` class (structured JSONL logging with fill dedup) extracted from `bot/_impl.py` (Bit 4.1, ~90-line, stdlib + `bot.constants` only). Re-exported into `bot/_impl.py` via `from bot.logger import Logger` so `MainLoop.__init__` instantiation + type annotations on `OpportunityScanner`/`OrderExecutor`/`SettlementTracker` resolve.
- `bot/notifier.py` — `TelegramNotifier` class (fire-and-forget Telegram alerts) extracted from `bot/_impl.py` (Bit 4.2, ~50-line, stdlib + `requests` only — zero `bot.constants` deps, zero `bot.helpers` deps). Re-exported into `bot/_impl.py` via `from bot.notifier import TelegramNotifier` so the runtime construction in `MainLoop.__init__` (now in `bot/main_loop.py` post-Bit-9.3 — search `self.telegram = TelegramNotifier`) resolves via the bot._impl re-export chain. **Bit 8.1 path-A++ (2026-05-10)**: also hosts the module-level `_TELEGRAM: Optional["TelegramNotifier"] = None` singleton (relocated from `bot/_impl.py`). Post-Bit-9.3 (2026-05-10), all five of `bot/_impl.py` (for the orphan-DB Layer-3 watchdog helpers — `_alert_orphan_db_holder` and the `detect_orphan_db_holders` lsof-not-found Telegram alert branch — the only remaining `_telegram_state._TELEGRAM` consumer block in bot/_impl.py post-MainLoop-extraction), `bot/main_loop.py` (MainLoop reads + the singleton WRITE in `__init__`), `bot/scanner/__init__.py`, `bot/executor.py` (Bit 9.1), and `bot/settlement.py` (Bit 9.2) reach it via `import bot.notifier as _telegram_state` plus `_telegram_state._TELEGRAM` module-attribute access — the access pattern preserves mutation freshness across all five consumers (parallel to the Bit 6.3 path-B `_cal_state._CALIBRATION_ENGINE` pattern). `MainLoop.__init__` writes via `_telegram_state._TELEGRAM = self.telegram`. The `Optional["TelegramNotifier"]` forward-ref on the singleton type annotation lives at the def site here and has no in-tree `typing.get_type_hints` consumer.
- `bot/kalshi_client.py` — `KalshiClient` class (Kalshi REST API auth + rate limiting + breaker-wrapped GETs + raw POST/DELETE writes) extracted from `bot/_impl.py` (Bit 4.3, ~388-line, stdlib + `requests` + `cryptography` + `bot.constants` (4 explicit names) + `bot.helpers.breakers` (4 explicit names) + `bot.infra.circuit_breaker.REGISTRY` (Sprint 10.5a relocation, 2026-05-11)). Re-exported into `bot/_impl.py` via `from bot.kalshi_client import KalshiClient` so MainLoop construction (`self.client = KalshiClient(...)`) + 7 type-annotation sites (`reconcile_with_api`, `_reconcile_positions`, `_reconcile_orders`, `OpportunityScanner.__init__`, `OrderExecutor.__init__`, `SettlementTracker.__init__`, `discover_active_windows()`) all resolve.
- `bot/fetchers/` — daemon-thread HTTP fetcher classes extracted from `bot/_impl.py` (Bit 4.4, ~237-line across 3 files: `__init__.py` re-export shim, `deribit.py` for `DeribitDVOLFetcher` (Deribit DVOL implied-vol index, BTC/ETH), `coinglass.py` for `CoinGlassFetcher` (CoinGlass funding rates for all of `bot.config.ASSETS` — DOGE/HYPE added in T1.5 bf8b9a3 2026-05-10 ticket 86b9vre9p; BNB added in T1.5 ticket 86b9zmj15 2026-05-17; disabled when `COINGLASS_API_KEY` env unset)). Re-exported into `bot/_impl.py` via `from bot.fetchers import DeribitDVOLFetcher, CoinGlassFetcher` so MainLoop construction (`self.dvol_fetcher = DeribitDVOLFetcher()`, `self.coinglass = CoinGlassFetcher()`) + the `Optional[DeribitDVOLFetcher]` type annotation on `VolatilityEngine.__init__` resolve. `DVOL_ANNUALIZED_TO_5S` (the only constant Bit 3.1 left in `bot/config.py` — relocated from repo-root `config.py` in Bit 12.1) is imported via `from bot.config import DVOL_ANNUALIZED_TO_5S` rather than `bot.constants`.
- `bot/feeds/` — WebSocket feed classes extracted from `bot/_impl.py` (Bit 4.5a + Bit 4.5b, ~2,580-line across 5 files: `__init__.py` re-export shim, `coinbase.py` for `CoinbaseFeed` (Coinbase WS spot prices for all of `bot.config.ASSETS` — DOGE/HYPE added in T1 2026-05-10 commit 5dca85a; BNB added in T1 2026-05-17 commit fbfc25d ticket 86b9zmj0c — with persistent 30-min snapshot buffer; Bit 4.5a. **D2.3 SHIPPED 2026-05-17, ticket `86b9zkppt`** refactored CoinbaseFeed to consume `coinbase_wire.ws_client.WSClient` — the asyncio event loop / websockets.connect / silence watchdog / frame parse moved to the wire library; CoinbaseFeed now wraps WSClient with 3 sync callbacks + a dedicated sampler daemon thread that replaces the pre-D2.3 asyncio `_snapshot_loop` coroutine. Subscribe scope narrowed to `channels=("ticker",)` (bot doesn't need matches/heartbeat/status — those are CPU/GIL noise; collector keeps the wider 4-channel set for bronze). The `_swallow_persist_exception` module-level helper was DELETED in the same Bit — the sampler thread runs persist directly under its own try/except, no longer needing the `asyncio.to_thread` done-callback), `orderbook_schema.py` for `OrderbookSchemaError` (exception raised by KalshiFeed on Kalshi WS schema migrations; Bit 4.5a), `cross_exchange.py` for `CrossExchangeFeed` (Binance/Kraken/Bybit WS feeds for lead/lag detection — takes a `CoinbaseFeed` reference at construction time via `from bot.feeds.coinbase import CoinbaseFeed`; Bit 4.5a), `kalshi.py` for `KalshiFeed` (Kalshi WS feed for fill notifications + per-ticker orderbook snapshots/deltas — largest leaf in the Sprint 4 modularization track at ~1,790-line; uses `from bot.feeds.orderbook_schema import OrderbookSchemaError` for the sibling exception; Bit 4.5b)). Re-exported into `bot/_impl.py` via `from bot.feeds import CoinbaseFeed, CrossExchangeFeed, KalshiFeed, OrderbookSchemaError` so MainLoop construction (`self.feed = CoinbaseFeed()`, `self.cross_feed = CrossExchangeFeed(self.feed) if CROSS_EXCHANGE_ENABLED else None`, `self.kalshi_feed = KalshiFeed(api_key, self.client.private_key)`) + `feed: CoinbaseFeed` type annotations on `VolatilityEngine.__init__` (now in `bot/engines/volatility.py`) and `OpportunityScanner.__init__` resolve. `ASSETS` (Bit 3.1 left in `bot/config.py` — relocated from repo-root `config.py` in Bit 12.1) imported via `from bot.config import ASSETS` by `coinbase.py` + `cross_exchange.py`.
- `bot/engines/` — math-layer engine classes extracted from `bot/_impl.py` (Bit 6.1 + 6.2 + 6.3 shipped — Sprint 6 closes here; ~2,170-line across 4 files: `__init__.py` re-export shim, `volatility.py` for `VolatilityEngine` (Realized Kernel + Deribit DVOL volatility engine with adaptive jump detection and EGARCH variance-space blending; ~960-line; Bit 6.1, byte-for-byte), `probability.py` for `ProbabilityEngine` (Student-t / NIG win-prob CDF + adaptive calibration cascade with BLR clamp + model-vs-market sanity gate; ~197-line, 5 staticmethods; Bit 6.2), `calibration.py` for `CalibrationEngine` (adaptive 3-method calibrator: Platt / Beta / BLR + STC-aware Platt + temperature-scaling shadow pipeline + lifecycle methods _load_state/_save_state/load_training_data_from_db; ~1,004-line class body, 29 methods, 2 staticmethods; **Bit 6.3 path-B**: byte-for-byte class body PLUS singleton/helper relocation — the `_CALIBRATION_ENGINE` singleton, `_CAL_REGISTRY` dict, and `_derive_subtype`/`_derive_asset_filter`/`_resolve_cal_engine` helpers live alongside the class in this file)). Re-exported into `bot/_impl.py` via `from bot.engines import VolatilityEngine, ProbabilityEngine, CalibrationEngine` so MainLoop construction (`self.vol = VolatilityEngine(self.feed, dvol_fetcher=self.dvol_fetcher, egarch_estimator=..., mz_tracker=...)` + the 3 `CalibrationEngine(...)` instantiation sites: 1× legacy 15M `_CALIBRATION_ENGINE` singleton + 2× per-product `_CAL_REGISTRY` populator) + the `vol: VolatilityEngine` type annotation on `OpportunityScanner.__init__` + the bare-name `ProbabilityEngine.X(...)` call sites in `bot/_impl.py` (scan-loop edge computation, counterfactual probability, dynamic cap lookup) all resolve. **`volatility.py` imports**: 35 explicit names from `bot.constants` + 5 from `bot.config` (ASSETS, EGARCH_BLEND_LOG_INTERVAL, EGARCH_BLEND_SHADOW_MODE, EGARCH_RV_RATIO_CLAMP, VOL_RETURN_INTERVAL — all pre-Bit-3.1 constants that stayed in `bot/config.py` (relocated from repo-root `config.py` in Bit 12.1)) + `compute_tv_rk_weights` from `models` + `CoinbaseFeed` from `bot.feeds.coinbase` + `DeribitDVOLFetcher` from `bot.fetchers.deribit` (the two type-annotation pins per L33). `Optional['EGARCHEstimator']` and `Optional['MincerZarnowitzTracker']` remain string-quoted forward refs because both classes still live in `models.py`. **`probability.py` imports**: 5 explicit names from `bot.constants` (DISCREPANCY_PRICE, DISCREPANCY_PROB, DYNAMIC_CAP_SCHEDULE, FIFTEEN_M_CALIBRATION_ENABLED, HOURLY_DYNAMIC_CAP_SCHEDULE) + 4 from `bot.config` (BETA_SLOPE, DIST_CONFIG, MAX_EFFECTIVE_PROB, STUDENT_T_DF — all pre-Bit-3.1 constants that stayed in `bot/config.py` (relocated from repo-root `config.py` in Bit 12.1); the L39 partition was the Plan-agent CRITICAL catch in pre-flight) + `get_market_config` from `market_config` + `student_t` (aliased from `t`) and `norminvgauss` from `scipy.stats` (the only `bot/engines/` module that imports scipy; the per-module forbidden-imports gate in `tests/contracts/test_engines_extraction.py` allows it for probability while still banning numpy/torch/sklearn/pandas) + `from bot.engines import calibration as _cal_state` (path-B: replaces the Bit 6.2 method-body late-binding `from bot import _impl as _bot_impl`). **`calibration.py` imports**: 10 explicit names from `bot.constants` (`CALIBRATION_BRIER_WINDOW`, `CALIBRATION_MIN_SAMPLES_BETA`, `CALIBRATION_MIN_SAMPLES_BLR`, `CALIBRATION_MIN_SAMPLES_PLATT`, `CALIBRATION_RETRAIN_INTERVAL`, `CALIBRATION_STATE_PATH`, `MARKET_BLEND_W`, `MIN_EDGE_PCT`, `SHADOW_BLEND_W`, `SHADOW_CAL_PIPELINE`) + 3 from `bot.config` (`BETA_SLOPE`, `MAX_EFFECTIVE_PROB`, `NUMERICAL_SAFETY_CEILING` — same pre-Bit-3.1 leftover-in-config pattern as Bits 6.1 and 6.2; pre-flight L39 verified) + `get_cal_excluded_types` + `get_market_config` from `market_config` + `calculate_taker_fee` from `models` + stdlib (`math`, `os`, `time`, `json`, `logging`, `datetime`, `timezone`, `deque`, `Optional`, `Dict`). **Bit 6.3 path-B singleton + helper relocation (2026-05-10)**: `_CALIBRATION_ENGINE` (typed `Optional[CalibrationEngine]` — bare, no quotes, since the class is now in scope), `_CAL_REGISTRY` (typed `Dict[str, CalibrationEngine]`), `_derive_subtype`, `_derive_asset_filter`, `_resolve_cal_engine` ALL moved from `bot/_impl.py` into `bot/engines/calibration.py` alongside the class. The class itself never read these names (free-variable analysis returned zero hits); they're written by `MainLoop.__init__` (3 instantiation sites) and read by callers OUTSIDE the class — `OpportunityScanner.scan()` and downstream sites. Both `bot/_impl.py` and `bot/engines/probability.py` reach them via top-level `from bot.engines import calibration as _cal_state` plus `_cal_state.X` attribute access — module-attribute access preserves singleton-mutation freshness without late-binding. The path-B move LIFTED the Bit 6.2 `from bot import _impl as _bot_impl` late-binding inside ProbabilityEngine and REMOVED the matching `.importlinter` `bot.engines.probability -> bot._impl` ignore_imports carve-out (Pillar 2). Sprint 6 closes here.
- `bot/scanner/__init__.py` — `OpportunityScanner` class (the trading hot path; ~9,085-line class body, 36 methods + 7 staticmethods) extracted from `bot/_impl.py` (Bit 8.1, 2026-05-10). Strict-ban for torch/sklearn/pandas (zero direct numerical imports; numpy/scipy reached transitively through `models.PositionSizer` etc.). Re-exported into `bot/_impl.py` via `from bot.scanner import OpportunityScanner` so MainLoop construction (`self.scanner = OpportunityScanner(..., main_loop=self)`) + 13 consumer call sites (12 `OrderExecutor.X` static-method calls + 1 MainLoop static-method call) + ~30 test-suite instantiation/`bot.OpportunityScanner.X` references all resolve via the proxy chain. **Path-A++ extraction (NOT byte-for-byte)**: in-Bit relocation of `_TELEGRAM` module-level singleton from `bot/_impl.py` to `bot/notifier.py` (with the `_telegram_state._TELEGRAM` module-attribute access pattern shared between this module and `bot/_impl.py`). **Post-Bit-9.1 (2026-05-10)**: the `_get_order_executor()` late-binding helper for the 34 `OrderExecutor.X(...)` static-method call sites RETIRED — scanner now uses top-level `from bot.executor import OrderExecutor` directly; `scanner-no-impl-toplevel` `.importlinter` contract dropped (net contracts: 6 → 5). **Imports**: 205 explicit names from `bot.constants` + 8 from `bot.config` (ASSETS, DRAWDOWN_HALF/HALT/QUARTER_THRESHOLD, EGARCH_BLEND_SHADOW_MODE, MAX_RISK_PER_TRADE, NUMERICAL_SAFETY_CEILING, SIZING_TIERS) + 9 from `bot.helpers` (cell_blocks/sizing/strategy/strings/tm_sweep helpers — explicit per-leaf imports replacing `from bot.helpers import *` star-import laundering per L40 lesson) + `KalshiClient` from `bot.kalshi_client` (type annotation) + `StateManager` from `bot.state` (type annotation + heavy method-call surface) + `CoinbaseFeed` from `bot.feeds` (type annotation) + `VolatilityEngine` (type annotation) + `ProbabilityEngine` (15 bare-name `ProbabilityEngine.X(...)` static-method call sites — scan-loop edge computation, counterfactual prob, dynamic cap) from `bot.engines` + `from bot.engines import calibration as _cal_state` (16 `_cal_state._CALIBRATION_ENGINE` / `._resolve_cal_engine` reads — Bit 6.3 path-B alias) + `Logger` from `bot.logger` (type annotation) + `import bot.notifier as _telegram_state` (Bit 8.1 path-A++ alias for `_telegram_state._TELEGRAM` reads) + `tracked_write` from `bot.db_writer_registry` (instrumentation) + `_calmlp_predictors`, `annotate_evaluation_async_enqueue as _calmlp_annotate_async` from `integration` (mirrors bot/_impl.py:67) + `PositionSizer`, `calculate_fee`, `calculate_taker_fee`, `strategy_to_group` from `models` + `get_market_config`, `validate_market_configs` from `market_config` + stdlib (datetime, deque, inspect, json, logging, math, os, random, re, sys, threading, time, typing). The `Optional[OrderFlowEngine]` and `Optional[KalshiOrderFlowTracker]` annotations in `__init__` are UNQUOTED post-Bit-9.3.5 (2026-05-10) — both classes live in `bot/order_flow.py` (search anchor: `from bot.order_flow import OrderFlowEngine, KalshiOrderFlowTracker` near the existing leaf-class import block); bot/order_flow.py is a clean leaf with zero bot.scanner edges so the top-level import resolves cleanly at scanner load time. **`.importlinter` carve-out (RETIRED in Bit 9.1)**: the `scanner-no-impl-toplevel` contract was added in Bit 8.1 (parallel to Bit 7.1's `state-no-impl-toplevel`) but DROPPED in Sprint 9 Bit 9.1 atomically with the helper retirement (net contracts: 6 → 5). Sister Bit 8.2 (`bot/scanner/CLAUDE.md`) ships separately per master plan; Bit 8.3 internal scanner split (into `discover.py` / `evaluate.py` / `gates.py` / `shadow.py`) deferred 7d post-Bit-8.1 per master plan. Sprint 8 closes here.
- `bot/state.py` — `StateManager` class (SQLite-backed persistent state for positions/orders/evaluated_opportunities/settled_trades/etc., 2,607-line class body, 38 methods, 2 staticmethods) extracted from `bot/_impl.py` (Bit 7.1, 2026-05-10). Strict-ban for numerical imports (zero numpy/scipy/torch/sklearn/pandas — pure stdlib + sqlite3). Re-exported into `bot/_impl.py` via `from bot.state import StateManager` so `MainLoop.__init__`'s `self.state = StateManager()` + 3 consumer-class type annotations (`OpportunityScanner.__init__`, `OrderExecutor.__init__`, `SettlementTracker.__init__`: `state: StateManager`) + ~50 test-suite instantiation sites resolve. **Path-A++ extraction (NOT byte-for-byte)**: in-Bit refactor of `scripts/cal_mlp/integration.py::parity_assert(conn) -> tuple[str, int]` and `sizing_parity_assert(conn, *, rowid, compute_for_15m_main_path)` dropped their `bot_globals` parameter — the laundered-namespace coupling smell is fixed in-Bit per the modularization strategic goal. Post-Bit-9.3-iii.a (2026-05-11), the Bit 7.1 `_get_compute_for_15m_main_path()` late-binding helper inside `bot/state.py` is RETIRED — `compute_for_15m_main_path` was relocated to clean-leaf `bot/boot.py` and bot/state.py top-imports it directly (`from bot.boot import compute_for_15m_main_path`). The `.importlinter` `state-no-impl-toplevel` carve-out retired in the same atomic commit. **Bit 7.1 fu (Smell 4, ticket 86b9vhccw, 2026-05-10)**: `make_compute_for_15m_main_path` itself was subsequently refactored to drop its `bot_globals: dict` parameter — the closure now imports its 11 dependent names from `bot.constants` + `bot.config` (Bit 12.1, 2026-05-12 retargeted from repo-root `config`) inside the function body (mirrors path-A++). **Imports**: 5 explicit names from `bot.constants` (DB_PATH, OB_CACHE_EVICT_AGE_SECONDS, OB_CACHE_FRESHNESS_SECONDS, SOL_RESCUE_CONTRACT_CAP, STACKING_ENABLED) + 3 from `bot.db_writer_registry` (recent_writes, snapshot_active, tracked_write — load-bearing for the cf34b5c db-locked SLOW_BATCH_BREAKDOWN instrumentation surface) + `from bot.engines import calibration as _cal_state` (Bit 6.3 path-B alias used at 3 sites in the failure-path logger reading `_cal_state._CALIBRATION_ENGINE` and `_cal_state._resolve_cal_engine`) + `KalshiClient` from `bot.kalshi_client` (type annotation only on `reconcile_with_api`/`_reconcile_positions`/`_reconcile_orders`) + 5 explicit names from `bot.helpers` (compute_derived_features, compute_time_regime_features, dollars_str_to_cents, fp_str_to_int, tm_sweep_counterfactual_pnl — these came into `bot/_impl.py` via `from bot.helpers import *` star-import laundering pre-extraction; explicit per-leaf imports replaced the smell at extraction time per L40 lesson) + 5 from `integration` (CalMLPParityError, CalMLPSchemaError, migrate_schema, parity_assert, sizing_parity_assert — refactored in-Bit per path-A++) + 2 from `models` (calculate_fee, strategy_to_group) + stdlib (datetime, json, logging, random, re, sqlite3, threading, time, datetime.timezone, typing). Sister Bit 7.2 (`agent_docs/db_schema.md` refresh) shipped in same atomic commit — the schema doc's source-of-truth for the 17 tables is now `StateManager._create_tables` in this file. Sprint 7 closes here.
- `ops/kalshi-bot.service` — systemd unit, source of truth (installed via `ops/install.sh`); see `ops/CLAUDE.md`.
- `start.sh` — wrapper invoked by `ops/kalshi-bot.service` (venv + .env + `python -m bot`)
- `bot/_thread_env.py` — sets OMP/MKL/OpenBLAS thread caps. Post-Bit-9.3-iii.c (bot/_impl.py DELETED) the load-bearing order is `bot/__main__.py` imports `bot._thread_env` BEFORE `from bot.main_loop import MainLoop`; `bot/boot.py` also defensively `__import__("bot._thread_env")`s before numerical libs load transitively. Order is load-bearing per CLAUDE.md and AST-asserted by `tests/integration/test_cal_mlp_invariants.py::test_thread_env_imported_before_numerical_libs_in_bot_boot` (the sister `..._in_bot_impl` test self-skips since bot/_impl.py no longer exists). (Pre-Bit-2.3 the file lived at `scripts/cal_mlp/_thread_env.py` and required a sys.path.insert to locate; Bit 2.3 moved it into the `bot/` package and retired the pre-_thread_env hack — though `scripts/cal_mlp/` is still added to sys.path post-_thread_env for `from integration import` calls.)

Engines (separate threads/processes):
- `bot/engines/spx_engine.py` — SPX hourly (Polygon, EGARCH, RK, VIX). Relocated from repo root in Sprint 10.1b (2026-05-11).
- `bot/engines/weather_engine.py` — weather ensemble (Open-Meteo GFS/ECMWF). Relocated from repo root in Sprint 10.1c (2026-05-11).
- `bot/engines/sports_engine.py` — sports comeback (ESPN, Bayesian posterior). Relocated from repo root in Sprint 10.1d (2026-05-11). **Sprint 10.1 engines/ sibling-reorg complete** (all 4 sub-moves shipped: sports_data 10.1a, spx_engine 10.1b, weather_engine 10.1c, sports_engine 10.1d).
- `bot/engines/sports_data.py` — sports data fetcher (relocated from repo root in Sprint 10.1a, 2026-05-11; first sub-move of the Sprint 10 engines/ sibling-reorg)

Shadows (observation-only):
- `bot/shadows/fifteenm_shadow.py` — 15M variants A1/A2/A3/A4 (relocated Sprint 10.2, 2026-05-11)
- `bot/shadows/hourly_alt_shadow.py` — hourly alternate sims (relocated Sprint 10.2, 2026-05-11)
- `bot/shadows/spx_harrv_shadow.py` — SPX HAR-RV shadow (relocated Sprint 10.2, 2026-05-11)

AI helpers (`bot/ai/`, relocated Sprint 10.3, 2026-05-12; standalone Telegram-driven entrypoints, NOT imported by bot runtime hot path; `auditor.py` + `researcher.py` anchor `__file__`-derived paths — `state.db`, `auditor_state.db`, `researcher_state.db`, `.env`, journals — to repo root via a 3-level dirname chain mirroring `bot/snapshots/supabase_sync.py:30`; `analyst.py` uses CWD-relative `state.db` literal — VPS cron invokes from repo root, so the literal still resolves there post-move; same risk class as the deferred-then-closed Sprint 14-A Bit X.5 `watchdog.py` → `ops/watchdog.py` move (2026-05-17)):
- `bot/ai/analyst.py` — Claude API loss analysis + news sentiment
- `bot/ai/auditor.py` — hourly deterministic health checks → Telegram alerts
- `bot/ai/researcher.py` — 3×/day performance reports → Telegram

Snapshots/sync (`bot/snapshots/`, relocated Sprint 10.4, 2026-05-12; helpers-leaf-listed as a forbidden subpackage; each module anchors `__file__`-derived paths — `state.db`, `.supabase_kill_switch` — to repo root via a 3-level dirname chain mirroring `bot/engines/weather_engine.py:806-807`; the `dist_config.json` reader appends `ops/runtime/` to that anchor post Sprint 14-A Bit 3 (2026-05-17, ticket 86b9zfbt8)):
- `bot/snapshots/dashboard_snapshot.py` — Supabase syncer (paired with `dashboard/index.html` on `gh-pages`)
- `bot/snapshots/bot_state_snapshot.py` — bot microstate forward-capture
- `bot/snapshots/market_observations_snapshotter.py` — NBBO continuous snapshotter
- `bot/snapshots/supabase_sync.py` — Postgres mirror

Infra:
- `bot/infra/capital_allocator.py`, `bot/infra/circuit_breaker.py` (relocated Sprint 10.5a, 2026-05-11), `bot/models.py` (relocated Sprint 10.5b, 2026-05-11 — sibling under bot/, NOT under bot/infra/; helpers-leaf carve-out for bot.helpers.tm_sweep -> bot.models), `ops/watchdog.py` (relocated Sprint 14-A Bit X.5, 2026-05-17 — STATE_FILE + DB_PATH bumped to `Path(__file__).parent.parent` to keep `.watchdog_state.json` + `state.db` lookups anchored at repo root; `ops/__init__.py` added so `import ops.watchdog` resolves from the test suite; Usage docstring + VPS crontab updated to `python3 ops/watchdog.py`)

Config:
- `market_config.py` — MarketTypeConfig dataclass; asserts against bot/_impl.py at startup
- `bot/config.py` (Bit 12.1, 2026-05-12 — relocated from repo-root `config.py`) hosts SIZING_TIERS / DRAWDOWN_* / EGARCH bounds / MAX_RISK_PER_TRADE / ASSETS / DIST_CONFIG / VOL_RETURN_INTERVAL etc. `config.json` (auto-generated by `scripts/ops/extract_config.py`) is still at repo root; `dist_config.json` (calibration output written by `scripts/ops/calibrate_dist.py`) lives at `ops/runtime/dist_config.json` post Sprint 14-A Bit 3 (2026-05-17, ticket 86b9zfbt8) — both remain DATA artifacts (NOT Python source). Master plan Phase II's "full collapse into bot/config.py" was structurally wrong on those 2 of 3 files; Bit 12.1 = Phase II-narrow. Sprint 14-A Bit 3 = data-artifact relocation, not collapse.

Operator scripts: `scripts/{audit,backfill,ops}/` (33+9+32 files; subdir reorg shipped Bit 11.2 2026-05-12). `scripts/cal_mlp/` and `scripts/git_hooks/` were already subdirs. Root allow-list: `vps_mcp_server.py` (Bit 13.5), `CLAUDE.md`, `STATE_DB_BACKUP_SETUP.md`, `VPS_SETUP.md`, `io.kalshi.state-db-backup-heartbeat.plist.template`, `collector-start.sh` (D1.5 wires `ops/kalshi-collector.service` ExecStart to it; sources `/home/botuser/.env.collector`), and `coinbase-collector-start.sh` (D2.5 wires `ops/kalshi-coinbase-collector.service` ExecStart to it; sources `/home/botuser/.env.coinbase-collector`; ticket `86b9znq4w`).

Data Corpus collector (`collector/`, top-level SIBLING to `bot/`; D1.1 SHIPPED 2026-05-16, ticket `86b9ypn49`; Data Corpus initiative `kb/decisions/data-corpus-architecture.md`). Structural bot-isolation contract enforced by `[importlinter:contract:collector-no-bot]` in `.importlinter` + AST defense-in-depth in `tests/contracts/test_collector_no_bot_imports.py`. ZERO `bot.*` imports. **D1.1.5 SHIPPED 2026-05-16, ticket `86b9zdhz2`** — `collector/auth.py` DELETED (auth flows through `kalshi_wire.auth` per the 2026-05-16 §5 AMENDMENT; "two sides of the same coin"); `collector/ws_connection.py` rewritten from docstring stub to `BronzeArchiver` thin-consumer of `kalshi_wire.ws_client.WSClient`. 8 files (down from 9 post-D1.1.5's auth.py deletion). D1.2 (2026-05-16, ticket `86b9ypn66`) shipped writer.py + uploader.py + main_loop.py body + ws_connection.py BronzeArchiver.run() body. **D1.3 SHIPPED 2026-05-16, ticket `86b9ypn72`** — subscription_manager.py body + BronzeArchiver.on_session_start dispatch + sid→channel binding + main_loop multi-conn fan-out; first-bronze-flow lands here. **D1.4 SHIPPED 2026-05-16, ticket `86b9ypn8r`** — `rest_snapshot.py` body wired (Kalshi REST `/markets` paginated fetch via `kalshi_wire.auth.make_rest_headers` + `RestSnapshotRefresher` hourly poll + `BronzeArchiver.update_subscriptions` / `request_reconnect` surface + `main_loop._replan_for_archivers` callback that rebuilds per-conn subscribes + force-reconnects on REST-detected ticker-set change). The hourly REST refresh is the new default; `COLLECTOR_TICKERS_FILE` retained as the offline/test boot seam. **D1.5 SHIPPED 2026-05-16, ticket `86b9ypna4`** (REQUIRES-APPROVAL discipline tier) — `ops/kalshi-collector.service` written + `ops/install.sh` extended to a multi-unit installer (parallel-array form: validates BOTH `kalshi-bot.service` and `kalshi-collector.service` then installs/enables both atomically; per-unit env-file check distinguishes the bot's repo-rooted `.env` from the collector's home-rooted `.env.collector`). `collector-start.sh` body refreshed to source `/home/botuser/.env.collector` (NOT the shared bot `.env`) per D0.3 §6 isolation strengthening. 3 D0.3 §12 operator decisions resolved at kickoff: `Restart=on-failure` + `RestartSec=10s` + `Nice=10` lifecycle posture; lifecycle Standard → DEEP_ARCHIVE @ 30d (skip IA); `KALSHI_COLLECTOR_KEY_ID` provisioned via operator runbook into `.env.collector`. Bronze day-zero is the first-chunk-in-S3 timestamp after `systemctl start kalshi-collector`. D1.5 surface lives at `ops/kalshi-collector.service` + `ops/install.sh` + `collector-start.sh` + `ops/CLAUDE.md` "D1.5 collector deploy" section + `scripts/STATE_DB_BACKUP_SETUP.md` §2/§2b/§3/§5 6-prefix templates; the `collector/` runtime code is unchanged at D1.5 — last shipped at D1.4 — though `collector/__init__.py` + `collector/main_loop.py` carry D1.5 docstring + error-message updates lockstep with the deploy:
- `collector/__init__.py` — package marker + docstring contract
- `collector/__main__.py` — entrypoint shim (mirrors `bot/__main__.py` sacred-boundary rule); imports `collector.main_loop.run`
- `collector/main_loop.py` — **D1.2 SHIPPED + D1.3 SHIPPED + D1.4 SHIPPED 2026-05-16 + D1.6 fu SHIPPED 2026-05-17, ticket `86b9zkktr`**; `run()` orchestrates SubscriptionManager + per-conn writers + archivers + uploader in multi-conn per-channel shape. Env-driven config (`KALSHI_COLLECTOR_KEY_ID`, `KALSHI_COLLECTOR_KEY_PATH`, `COLLECTOR_BRONZE_ROOT`, `COLLECTOR_CONN_COUNT`, `COLLECTOR_TICKERS_FILE`, `COLLECTOR_BATCH_SIZE`, `COLLECTOR_REST_REFRESH_SECONDS`, `COLLECTOR_HEALTH_SIDECAR_PATH` (D1.6 fu, default `/var/lib/kalshi-collector/bronze_health.json`), `RCLONE_REMOTE`, `S3_BUCKET`). D1.6 fu added `write_bronze_health_sidecar(archivers, path)` invoked by the drain thread each tick — atomic-replace JSON write aggregating per-archiver `BronzeArchiver.get_health_snapshot()` (7 keys post-D1.3-fu5: conn_id, dropped_frames, write_queue_size, write_queue_maxsize, write_worker_alive, collector_seq, ack_frames_processed) for the cron-driven `scripts/ops/collector_health_monitor.py::check_dropped_frames` reader. Sidecar `schema_version` stays at 1 (the `ack_frames_processed` key is additive backward-compat). Per-conn: 4 BronzeWriters (3 channels + `_unrouted` None-channel fallback) + 1 BronzeArchiver with pre-built subscribe_frames + cmd_id_to_channel. Single drain thread fans out across ALL writers' `rotated_outbox_paths`. Restart sweep BEFORE new rotations + graceful shutdown via threading.Event. D1.4 added the REST-fed path: when `COLLECTOR_TICKERS_FILE` is unset, `fetch_tickers_by_tier()` populates the initial subscription set at boot and `RestSnapshotRefresher` re-polls every `COLLECTOR_REST_REFRESH_SECONDS` (default 3600s); ticker-set changes trigger `_replan_for_archivers` which calls `archiver.update_subscriptions(...)` + `archiver.request_reconnect()` on every archiver.
- `collector/ws_connection.py` — **D1.1.5 SHIPPED + D1.2 SHIPPED + D1.3 SHIPPED + D1.4 SHIPPED 2026-05-16 + D1.3-fu3 SHIPPED 2026-05-17, ticket `86b9zjyr0` + D1.3-fu4 SHIPPED 2026-05-17, ticket `86b9zk4hz` + D1.3-fu5 SHIPPED 2026-05-17, ticket `86b9zky3u`**; `BronzeArchiver` instantiates `kalshi_wire.ws_client.WSClient(api_key, private_key, on_frame=..., on_session_start=..., on_session_end=..., ping_timeout=30.0)`. `on_session_start` dispatches every pre-built subscribe frame via `self._wire.send_frame` (per-frame try/except so a single failure doesn't abort the dispatch). `on_session_end` clears the `sid_to_channel` map (R3/P0-A invariant: sids are Kalshi-session-scoped). `on_frame` (asyncio thread, MUST return quickly) binds sid→channel from `type=subscribed` (sid in `msg.sid`) / `type=ok` (sid at top level) acks via `cmd_id_to_channel` lookup synchronously (binding must be visible to immediately-subsequent data frames in the same tick). **D1.3-fu5 amendment**: ack frames bind sid + increment `_ack_frames_processed` then RETURN — they are NOT enqueued for bronze writing (cumulative-ticker payloads reach ~5 MB per ack; queueing them at subscribe-burst rate OOM'd the cgroup 2026-05-17, see `kb/failures/collector-oom-via-ack-queue-may17.md`). Data frames continue: allocate per-frame `collector_seq` under lock (preserves monotonic ordering across the worker handoff), then **enqueue `(frame, channel, seq)` to a bounded `_write_queue` (default maxsize=10_000) for the in-process worker thread to dispatch via `_drain_loop`** — the worker builds the envelope + writes to `writers_by_channel[channel]` with `_unrouted` (None-key) fallback for unmapped sids. The `_unrouted/` bronze partition NO LONGER receives ack frames post-fu5. On queue.Full → `_dropped_frames` counter + throttled WARN, never block. `_wire_recv_ts` still captured at frame ingress per D0.3 §2 (R1-C1 invariant carried from D1.2; passed via the queue tuple so the worker dispatches with the ingress-time value, not worker-thread-time). `BronzeArchiver.run(shutdown_event=None)` installs SIGINT/SIGTERM handlers + blocks on event (non-main-thread tolerant). `start()` spawns the worker BEFORE starting the wire; `stop()` halts the wire FIRST via `WSClient.stop(join_timeout=30s)` (D1.3-fu4 also extended kalshi_wire's `WSClient.stop` to accept the `join_timeout` kwarg — without joining the asyncio thread, frames received during shutdown would land behind the sentinel and never drain), then posts a shutdown sentinel + joins the worker with a 30s bound. D1.3-fu3 raised `ping_timeout` from the kalshi_wire default (10s) to 30s as a stopgap against 1011 keepalive-ping-timeout reconnect storms; D1.3-fu4 closed the underlying class by moving the heavy `build_envelope` + `writer.write` work off the asyncio thread, and the 30s timeout is retained as defense-in-depth. D1.4 added `update_subscriptions(frames, cmd_id_map)` (atomic-replace of `_subscribe_frames` + `_cmd_id_to_channel` under `_lock`; readers in `_on_session_start` + `_handle_subscribe_ack` rely on single-bytecode-op attribute capture under the GIL — see the `update_subscriptions` docstring for the full safety model and a future-maintainer rule for multi-step iteration) + `request_reconnect()` (delegates to `WSClient.request_reconnect`) so the REST-refresh callback can push a new ticker set without recreating the archiver. **D1.6 fu** added `get_health_snapshot()` returning a 6-key dict (conn_id, dropped_frames, write_queue_size, write_queue_maxsize, write_worker_alive, collector_seq) consumed by `collector.main_loop.write_bronze_health_sidecar` for the cron-driven monitor. **D1.3-fu5** extended the dict to 7 keys (additive: `ack_frames_processed`) so the monitor can distinguish "collector healthy + receiving acks" from "collector wedged + no activity"; sidecar `schema_version` stays at 1 (backward-compat).
- `collector/rest_snapshot.py` — **D1.4 SHIPPED 2026-05-16, ticket `86b9ypn8r`** — REST-fed catalog refresh; `fetch_tickers_by_tier(api_key, private_key, ...)` GETs `/trade-api/v2/markets?status=open` paginated via cursor, dedupes + sorts, returns `{TIER_ALL: [tickers]}`. Auth via `kalshi_wire.auth.make_rest_headers` (no inline RSA-PSS). Non-raising: transport errors / 5xx / 429 retry with exponential backoff up to `max_retries=3`; non-429 4xx + malformed JSON + partial pagination return `None` (caller keeps prior subscription map; never wipes a live deploy's set). `RestSnapshotRefresher` is the periodic-poll thread: first refresh fires immediately on `start()`, subsequent ticks every `interval_seconds` (default `DEFAULT_REFRESH_INTERVAL_SECONDS = 3600.0`). Callback fires ONLY on ticker-set change (no-op refreshes skipped to avoid reconnect storms). Callback exceptions logged + swallowed so a buggy on_refresh can't crash the thread. Single-tier classification (`TIER_ALL = "1"`) until a measurement-driven split is justified (D0.2 scope-map authoritative: all 59,904 non-MVE markets in scope).
- `collector/writer.py` — **D1.2 SHIPPED 2026-05-16, ticket `86b9ypn66`**; `BronzeWriter.write(envelope)` is the production seam (called by BronzeArchiver); `write_frame(wire_recv_ts, raw_payload)` is a convenience wrapper. Hive partition derived from frame `_wire_recv_ts` (D0.3 §3). Rotation 5min OR 100MB (D0.3 §4) with zstd-6 compression + POSIX-atomic rename. `_rotate` renames the in-flight `.jsonl` to share the chunk_id stem so the uploader's name-based pairing finds both halves. `rotated_outbox_paths: List[Tuple[outbox, in_flight]]` carries the pair explicitly.
- `collector/uploader.py` — **D1.2 SHIPPED 2026-05-16, ticket `86b9ypn66`**; `RcloneUploader.upload_chunk(outbox, in_flight, s3_key)` uses `rclone copyto` (NOT `rclone copy` — file-vs-dir disambiguation) with `--checksum --immutable`. KEEP-local on rclone non-zero exit OR size-verify byte-mismatch. Delete BOTH outbox + in_flight on success. `sweep_outbox(root_dir)` re-uploads leftovers on restart (D0.3 §7 last paragraph).
- `collector/subscription_manager.py` — **D1.3 SHIPPED 2026-05-16, ticket `86b9ypn72`**; `SubscriptionManager(tickers_by_tier, conn_count, channels=CHANNELS_DEFAULT, batch_size=1000)` + `assign() -> List[ConnPlan]` (round-robin tickers within each tier across N conns; conn_ids are A/B/C/D/E/F per D0.3 §3) + `build_subscribe_frames(plan, *, cmd_id_start, batch_size) -> (frames, cmd_id_to_channel)` (single-channel-per-frame so subscribe-ack cmd_id→channel resolution is unambiguous; mirrors bot/feeds/kalshi.py:`_send_ob_subscribe` shape). `CHANNELS_DEFAULT = ("orderbook_delta", "trade", "market_lifecycle_v2")` — `fill` intentionally absent (bot-only private channel).
- `collector/coinbase_archiver.py` — **D2.2 SHIPPED 2026-05-17, ticket `86b9zkppk` + D2.5 PROMOTED 2026-05-18, ticket `86b9znq4w`**; `CoinbaseArchiver(writers_by_channel, conn_id="A", url=None, channels=None, product_ids=None, msg_type_to_channel=DEFAULT_MSG_TYPE_TO_CHANNEL, write_queue_maxsize=10_000)` — Coinbase-side mirror of `BronzeArchiver`, adapted for Exchange WS's single-conn + static `msg_type → channel` dispatch (no sids / no cmd_id binding analog). Post-D2.5 `DEFAULT_MSG_TYPE_TO_CHANNEL` maps 6 msg-types across 5 channels: `ticker → ticker`, `match → matches`, `heartbeat → heartbeat`, `status → status`, `snapshot → level2_batch`, `l2update → level2_batch` (the level2 entries landed at D2.5 alongside the `coinbase_wire.DEFAULT_CHANNELS` extension; the R0 reachability spike at D2.5 kickoff confirmed `level2_batch` is publicly subscribable — 1 snapshot + 502 l2update frames over 30s for BTC-USD alone, no `type=error`). Unmapped / missing `msg_type` routes to the `None`-keyed `_unrouted` writer so bronze captures the bytes for silver QA. Every envelope carries `_source="coinbase_ws"`. Constructs `coinbase_wire.ws_client.WSClient(on_frame=..., on_session_start=..., on_session_end=..., url=..., channels=..., product_ids=...)` with explicit kwargs (AST contract test verifies wiring). `_on_session_start` builds the Coinbase Exchange WS subscribe payload via the public `coinbase_wire.auth.build_public_subscribe_message(channels, product_ids)` helper and dispatches it through the public `WSClient.send_frame` API (R1-M1: decoupled from the wire library's private `_default_on_session_start` to avoid silent-partial-failure on wire-side rename — an AST guard `test_on_session_start_does_not_reach_into_private_wire_method` forbids any `self._wire._<single-underscore>` reference inside `_on_session_start`); `_on_session_end` is a no-op placeholder for the R3/P0-A symmetry (Coinbase has no per-session sid state to clear). Applies D1.3-fu4 worker-thread decouple + D1.3-fu5 skip-ack-enqueue FROM DAY 1: ack-class frames (`_SUBSCRIBE_ACK_TYPES = frozenset({"subscriptions", "error"})`) bump `_ack_frames_processed` then RETURN (no seq alloc, no enqueue); data frames allocate `collector_seq` under lock then `put_nowait` to a bounded `queue.Queue` (default maxsize=10_000) for the daemon worker thread to dispatch via `_drain_loop`. On queue.Full → `_dropped_frames` counter + throttled WARN, never block. `get_health_snapshot()` mirrors `BronzeArchiver`'s 7-key shape (conn_id, dropped_frames, write_queue_size, write_queue_maxsize, write_worker_alive, collector_seq, ack_frames_processed) so the cron-driven monitor can poll Coinbase + Kalshi tiers identically (D2.5 extends `collector_health_monitor.py` to dual-tier dispatch). `start()` spawns the worker BEFORE the wire; `stop()` halts the wire FIRST via `WSClient.stop(join_timeout=30s)`, then posts a shutdown sentinel + joins the worker.
- `collector/coinbase_main_loop.py` — **D2.5 SHIPPED 2026-05-18, ticket `86b9znq4w`**; `run(*, bronze_root=None, url=None, shutdown_event=None)` is the orchestrator the kalshi-coinbase-collector systemd unit invokes via `python3 -m collector.coinbase_main_loop`. Mirror of `collector/main_loop.py` stripped down for Coinbase: single-conn → no SubscriptionManager + no ConnPlan iteration; static `coinbase_wire.DEFAULT_PRODUCT_IDS` → no RestSnapshotRefresher; public channels at D2.1.5 → no PEM load + no `load_private_key` call. Env-driven config (`COINBASE_BRONZE_ROOT` default `/var/lib/kalshi-coinbase-collector/bronze`, `COINBASE_HEALTH_SIDECAR_PATH` default alongside bronze root, `RCLONE_REMOTE` default `s3prod`, `S3_BUCKET` default `kalshi-bot-archive`). Allocates 6 BronzeWriters (5 channels post-D2.5 level2_batch promotion + 1 None-keyed `_unrouted` fallback) all bound to `source="coinbase_ws"` + `conn="A"`, hands them to `CoinbaseArchiver` via `writers_by_channel`, starts a single drain thread that fans out across all writers AND writes the bronze_health.json sidecar each tick (separate file from Kalshi-side sidecar per the D2.5 Option B isolation posture). SIGINT/SIGTERM handlers when `shutdown_event` is owned; graceful shutdown sequences archiver.stop → writer.close × N → drain.join → final sweep. `if __name__ == "__main__": run()` block invokes the orchestrator under `python3 -m collector.coinbase_main_loop`; `logging.basicConfig(force=True)` is called from inside the `__main__` block (mirrors `collector/__main__.py` posture).

Shared transport library `kalshi_wire/` (top-level SIBLING to both `bot/` and `collector/`; D1.1.5 SHIPPED 2026-05-16, ticket `86b9zdhz2`; per 2026-05-16 AMENDMENT to `kb/decisions/data-corpus-architecture.md` §5 — supersedes the original "duplicate the minimum in collector" stance after external-advisor "two sides of the same coin" pivot). Pure-transport leaf — NO `bot.*` or `collector.*` imports (pinned by `[importlinter:contract:kalshi_wire-no-bot]` + `[importlinter:contract:kalshi_wire-no-collector]`):
- `kalshi_wire/__init__.py` — package marker + canonical Bit ⇄ submodule mapping
- `kalshi_wire/auth.py` — RSA-PSS-SHA256 `sign()` + `load_private_key()` + `make_rest_headers()` + `make_ws_headers()`. Replaces the historical inline construction in `bot/kalshi_client.py:60` + `bot/feeds/kalshi.py:768`. Byte-verifiable parity pinned by `tests/contracts/test_kalshi_wire_auth.py`.
- `kalshi_wire/ws_client.py` — `WSClient` class + `Frame` dataclass + `build_envelope()`. WSClient owns the asyncio thread, WS connect/reconnect with exponential backoff, RSA-PSS handshake auth, silence watchdog (Apr-24 ordering invariant: `_last_msg_ts` set BEFORE invoking `on_frame`), thread-safe outgoing-frame queue, frame parse + seq-gap detect. Consumers (`bot/feeds/kalshi.py` KalshiFeed + `collector/ws_connection.py` BronzeArchiver) supply 4 sync callbacks: `on_session_start`, `on_frame`, `on_session_end`, `on_drain_tick`. The R3/P0-A reconnect-cleanup invariant is preserved: `on_session_end` fires BEFORE the backoff sleep. Differential test `tests/equivalence/test_kalshi_wire_differential.py` pins byte-identical frame capture across the two consumers (Pillar 3 load-bearing).

Coinbase wire library `coinbase_wire/` (top-level SIBLING to `bot/`, `collector/`, and `kalshi_wire/`; **D2.1 SCAFFOLDING SHIPPED 2026-05-17, ticket `86b9zkpc6`** + **D2.1.5 BODY SHIPPED 2026-05-17, ticket `86b9zkpny`** — both sub-Bits of the `86b9zkkv4` D2.x Coinbase WS bronzing umbrella). Mirrors the D1.1.5 `kalshi_wire/` "two sides of the same coin" pattern for the Coinbase wire surface. **Protocol surface is Coinbase Exchange WS** (`wss://ws-feed.exchange.coinbase.com`) — the same endpoint the bot already consumes via `bot/feeds/coinbase.py` (see `bot.constants.COINBASE_WS_URL`); the shared-endpoint choice let **D2.3 SHIPPED 2026-05-17, ticket `86b9zkppt`** be a structural refactor (point the existing bot feed at this WSClient) rather than a protocol-flip. Coinbase Advanced Trade WS (`wss://advanced-trade-ws.coinbase.com`) is a separate API surface not targeted by D2.1.5. D2.1.5 NARROWED the original D2.1 forecast (HMAC-SHA256 for private channels) to **PUBLIC CHANNELS ONLY** per the operator scope decision at D2.1.5 kickoff — bronzing public WS data closes the non-reproducible Coinbase orderbook training-data gap without provisioning Coinbase API credentials. HMAC stubs remain as sentinel `NotImplementedError` guards for a future private-channel Bit. Pure-transport leaf — NO `bot.*` or `collector.*` imports (pinned by `[importlinter:contract:coinbase_wire-no-bot]` + `[importlinter:contract:coinbase_wire-no-collector]` + AST defense-in-depth in `tests/contracts/test_coinbase_wire_no_bot_imports.py` + `tests/contracts/test_coinbase_wire_no_collector.py`). **D2.2 SHIPPED 2026-05-17, ticket `86b9zkppk`** wires `collector/coinbase_archiver.py` (`CoinbaseArchiver` body — see the dedicated entry in the `collector/` files list below); **D2.3 SHIPPED 2026-05-17, ticket `86b9zkppt`** refactored `bot/feeds/coinbase.py` to consume the same WSClient (parallel of D1.1.5 Phase 3b for the Kalshi side). **D2.5 SHIPPED 2026-05-18, ticket `86b9znq4w`** ships the Coinbase collector's systemd unit (`ops/kalshi-coinbase-collector.service` Option B isolation), `coinbase-collector-start.sh` wrapper, `collector/coinbase_main_loop.py` orchestrator (dedicated entry above), `ops/install.sh` extension to a 3-unit installer, `.github/workflows/deploy.yml` path-aware kalshi-coinbase-collector restart block, and `scripts/ops/collector_health_monitor.py` dual-tier dispatch — see the dedicated Process topology paragraph below. D2.5 also bundled the level2_batch promotion: the R0 reachability spike at D2.5 kickoff confirmed `level2_batch` is publicly subscribable on the Exchange WS endpoint (1 snapshot + 502 l2update frames over 30s for BTC-USD alone, no `type=error`). Post-D2.5 default channel set is 5 verified-public Coinbase Exchange WS channels (`ticker` + `matches` + `heartbeat` + `status` + `level2_batch`); the matching `collector.coinbase_archiver.DEFAULT_MSG_TYPE_TO_CHANNEL` dispatch table extends with `snapshot → level2_batch` + `l2update → level2_batch`. Default product set: 7 entries — BTC / ETH / SOL / XRP / HYPE / DOGE / BNB — mirroring `bot.constants.COINBASE_PRODUCTS`. HYPE-USD verified live on Coinbase Exchange 2026-05-10 per `bot/constants.py` comment; BNB-USD landed in main 2026-05-17 via the BNB T1-onboarding ship (PR #78, ticket `86b9zmj0c`) with the verification "status=online + trading_disabled=false" on Coinbase Exchange. Files:
- `coinbase_wire/__init__.py` — package marker + docstring referencing D0.3 §5 AMENDMENT + canonical Bit ⇄ submodule mapping. Top-level re-exports `Frame`, `WSClient`, `build_envelope`. `Frame` + `build_envelope` are the canonical bronze-data surface (D0.3 §2 6-field contract); `WSClient` is the transport class consumers instantiate to receive `Frame`s (mirrors `kalshi_wire/__init__.py` convention).
- `coinbase_wire/auth.py` — `build_public_subscribe_message(channels, product_ids)` returns the Coinbase Exchange WS subscribe payload (no signature; `type=subscribe`, `product_ids`, plural `channels` — Coinbase Exchange WS lets a single subscribe message batch multiple channels). `sign()` + `make_ws_headers()` are sentinel stubs raising `NotImplementedError` until a future private-channel Bit lands.
- `coinbase_wire/ws_client.py` — `WSClient` (asyncio-internal, sync public API, 4 sync callbacks `on_session_start` / `on_frame` / `on_session_end` / `on_drain_tick`) + `Frame` dataclass (`wire_recv_ts`, `raw`, `parsed`, `channel`, `msg_type`, `sequence_num`; Coinbase Exchange WS has no top-level `channel` field so `Frame.channel` is None at the wire layer, consumers map `msg_type` → channel; `sequence_num` is from Exchange WS's per-product `sequence` field, wire-level gap detection deferred to consumer which holds per-product context) + `build_envelope()` (identical D0.3 §2 6-field shape as kalshi_wire). Defaults: `DEFAULT_WS_URL = "wss://ws-feed.exchange.coinbase.com"`, post-D2.5 `DEFAULT_CHANNELS = (ticker, matches, heartbeat, status, level2_batch)` (D2.5 PROMOTED `level2_batch` after the R0 reachability spike verified public access), `DEFAULT_PRODUCT_IDS = (BTC-USD, ETH-USD, SOL-USD, XRP-USD, HYPE-USD, DOGE-USD, BNB-USD)`. Lessons carried forward from Kalshi-bronze: `ws_max_size=16 MiB` default (D1.3-fu1 cumulative-ack defense), `ping_timeout=30s` default (D1.3-fu3 1011 close-cycle defense). Default `on_session_start` callback dispatches one batched `build_public_subscribe_message` (Coinbase Exchange WS subscribes batch multiple channels in one frame).

Process topology (D0.3 §5): `systemd → kalshi-bot.service` AND `systemd → kalshi-collector.service` (D1.5 SHIPPED 2026-05-16) AND `systemd → kalshi-coinbase-collector.service` (D2.5 SHIPPED 2026-05-18, ticket `86b9znq4w`) — three parallel units, no shared imports across bot/collector boundary, no shared sockets, no shared API keys (`KALSHI_COLLECTOR_KEY_ID` lives in `/home/botuser/.env.collector`; `kalshi-coinbase-collector` carries no API key at D2.5 since public channels only — its `.env.coinbase-collector` carries only `COINBASE_BRONZE_ROOT` / `RCLONE_REMOTE` / `S3_BUCKET`), no shared `state.db`. Isolation knobs structurally enforced by `ops/kalshi-collector.service`: `CPUAffinity=1` (vCPU-1 pin; bot keeps vCPU-0), `Nice=10` (I/O-bound polite background), `MemoryMax=512M` + `MemorySwapMax=0` (kernel-kill before OOMing the 2GB box), `LimitNOFILE=4096`, `Restart=on-failure` + `RestartSec=10s` (NOT bot's `always` + `30s` — clean `systemctl stop` halts the collector deliberately for credential rotation). D2.5 Coinbase deltas (pinned by `tests/contracts/test_kalshi_coinbase_collector_systemd_unit.py`): NO `CPUAffinity` (the 2-vCPU VPS already has the bot implicit on vCPU-0 and kalshi-collector pinned to vCPU-1; pinning a third tenant over-constrains the scheduler — Coinbase single-conn light load is fine on either vCPU), `MemoryMax=256M` (Coinbase single-conn × 7 products × 5 channels is structurally lighter than Kalshi 7-conn × ~21K-subs; halve the cap), `LimitNOFILE=512` (1 conn × 5 channels × rotation + rclone needs ~30 fd typical; tighter bound surfaces fd-leak regressions early). Same as Kalshi-collector: `Nice=10`, `MemorySwapMax=0`, `Restart=on-failure` + `RestartSec=10s`, `EnvironmentFile=/home/botuser/.env.coinbase-collector` (DEDICATED, NOT shared with Kalshi env file). The 3-unit installer (`ops/install.sh`) parallel-array form validates BOTH .service file existence AND wrapper executability AND env-file presence AND directive-line accuracy for all three units atomically before any `sudo cp`; failure of any single unit aborts the install with the per-unit operator-runbook hint.

Tests: `tests/` (~4,190 collected post-Bit-6.3 — varies ~5 with hypothesis parameterization; verify with `pytest tests/ --collect-only -q | tail -1`. Bit 6.3 added CalibrationEngine cases in `tests/contracts/test_engines_extraction.py` — identity / drift-guard / forbidden-imports / module-level singleton-relocation pins (path-B) / behavioral module-attribute access regression — atop the Bit 6.1 + 6.2 surface. Pillar 3 added the `tests/equivalence/` engine-equivalence harness (24 tests, hypothesis + pytest-regressions snapshots). Tier subdirs (unit/contracts/equivalence/integration/regression) shipped in Bit 12.2, 2026-05-11.)

KB (local-only, never committed): `kb/`, `kb-research/`

## Modularization destination

Per `kb/decisions/repo-modularization-plan-may05.md`:
- Sprint 3 → constants + helpers extracted to `bot/constants.py` + `bot/helpers/*` (Bit 3.1 + Bit 3.2 + Bit 3.3 SHIPPED 2026-05-08)
- Sprint 4–6 → leaf classes (Bit 4.1 Logger SHIPPED 2026-05-08 → `bot/logger.py`; Bit 4.2 TelegramNotifier SHIPPED 2026-05-08 → `bot/notifier.py`; Bit 4.3 KalshiClient SHIPPED 2026-05-08 → `bot/kalshi_client.py`; Bit 4.4 fetchers (DeribitDVOLFetcher + CoinGlassFetcher) SHIPPED 2026-05-08 → `bot/fetchers/`; Bit 4.5a small feeds (CoinbaseFeed + OrderbookSchemaError + CrossExchangeFeed) SHIPPED 2026-05-08 → `bot/feeds/`; Bit 4.5b KalshiFeed SHIPPED 2026-05-09 → `bot/feeds/kalshi.py` — largest single leaf at ~1,790-line; Bit 6.1 VolatilityEngine SHIPPED 2026-05-09 → `bot/engines/volatility.py` — first leaf in new `bot/engines/` subpackage at ~960-line; Bit 6.2 ProbabilityEngine SHIPPED 2026-05-09 → `bot/engines/probability.py` — second leaf at ~197-line, first non-byte-for-byte extraction (late-binding pattern for mutable bot._impl singletons); Bit 6.3 CalibrationEngine SHIPPED 2026-05-10 → `bot/engines/calibration.py` — third leaf at ~1,004-line (29 methods, 2 staticmethods); **path-B refactor** moved the `_CALIBRATION_ENGINE` singleton + `_CAL_REGISTRY` dict + `_derive_subtype`/`_derive_asset_filter`/`_resolve_cal_engine` helpers alongside the class (lifting Bit 6.2's late-binding inside ProbabilityEngine + removing the `.importlinter` carve-out); Sprint 6 closes here)
- Sprint 7 → StateManager SHIPPED 2026-05-10 → `bot/state.py` — first peer-module top-level extraction (path-A++; in-Bit smell-fix on parity_assert/sizing_parity_assert in scripts/cal_mlp/integration.py); Sprint 7 closes here
- Sprint 8 → OpportunityScanner SHIPPED 2026-05-10 → `bot/scanner/__init__.py` — largest single-class extraction at ~9,085-line (path-A++; in-Bit `_TELEGRAM` singleton relocation to `bot/notifier.py` via `_telegram_state._TELEGRAM` module-attribute access pattern; the `_get_order_executor()` late-binding helper RETIRED in Bit 9.1 atomically). Bit 8.2 `bot/scanner/CLAUDE.md` shipped 2026-05-10; Bit 8.3 internal split (`discover.py`/`evaluate.py`/`gates.py`/`shadow.py`) deferred 7d post-Bit-8.1. Sprint 8 closed at Bit 8.2.
- Sprint 9 → OrderExecutor SHIPPED 2026-05-10 (Bit 9.1) → `bot/executor.py` — second-largest single-class extraction at ~5,413-line (path-A++; in-Bit `_append_raw_api_journal` relocation to `bot/helpers/raw_api_journal.py` via L81 alias-import; bot.executor ↔ bot.scanner cycle broken via `_get_opportunity_scanner()` method-body helper inside bot/executor.py; 4 latent `OpportunityScanner._best_ask_depth(...)` AttributeError sites fixed — closes ticket 86b9vn9r5). Sister cleanup: `_get_order_executor()` helper retired from bot/scanner, `scanner-no-impl-toplevel` `.importlinter` contract dropped (net contracts 6 → 5), 3 peer-pin tests dropped, EXPECTED_CONTRACTS shrunk to 5.
- Sprint 9 → SettlementTracker + discover_active_windows SHIPPED 2026-05-10 (Bit 9.2) → `bot/settlement.py` — 1,275-line file. Path-A++ — bundled discover_active_windows per master plan Phase Z+AA decision; the L81 alias-import for `_append_raw_api_journal` RETIRED atomically; bot/notifier.py docstring 3→4 consumers; no new `.importlinter` carve-out. Bundled bug fix: 86b9vppn3 best_ask UnboundLocalError.
- Sprint 9 → MainLoop SHIPPED 2026-05-10 (Bit 9.3-i) → `bot/main_loop.py` — 2,200-line file (1,999-line class body + 150-line header + late-binding inserts). Path-A method-body late-binding for ALL bot._impl access (no top-level edge; net `.importlinter` contracts stays at 5). Two-step atomic per master plan L2195-2216 — 9.3-i preserved bot/__main__.py UNCHANGED; 9.3-ii (separate Bit) swaps bot/__main__.py to direct `from bot.main_loop import MainLoop`. `_telegram_state._TELEGRAM` consumer count grew 4→5 (bot/_impl.py STAYS for `_alert_orphan_db_holder` orphan-DB helper; bot/main_loop.py ADDS).
- Sprint 9 → OrderFlowEngine + KalshiOrderFlowTracker SHIPPED 2026-05-10 (Bit 9.3.5) → `bot/order_flow.py` — clean leaf 378-line file (smallest Sprint 9 extraction). Stdlib + 21 explicit bot.constants names only; zero `_telegram_state` or `_cal_state` consumers; zero bot._impl edges. The two `# REMOVE BIT 9.3.5` markers in MainLoop.__init__'s late-binding block collapsed to a top-level `from bot.order_flow import` in bot/main_loop.py. Sister cleanup atomic in same commit: bot/scanner/__init__.py `Optional["OrderFlowEngine"]` / `Optional["KalshiOrderFlowTracker"]` forward-refs UNQUOTED. `.importlinter` `helpers-leaf` `forbidden_modules` extended with `bot.order_flow`; net contracts stays at 5. 5-consumer `_telegram_state._TELEGRAM` enumeration UNCHANGED. **Sprint 9 closes here.**
- Sprint 9 → Bit 9.3-ii SHIPPED 2026-05-10 — orphan-DB Layer-3 watchdog → NEW `bot/orphan_db_watchdog.py` clean leaf (5 functions + `_ORPHAN_DB_WATCHDOG_PATTERNS` list, ~200 LOC, was bot/_impl.py:375-578). bot/__main__.py SWAPPED to direct `from bot.main_loop import MainLoop` with `import bot._thread_env` as FIRST import (preserves OMP_NUM_THREADS=1-before-numpy guarantee post-swap). MainLoop.startup() late-binding retargeted from bot._impl to bot.orphan_db_watchdog. 5-consumer `_telegram_state._TELEGRAM` enumeration: bot/orphan_db_watchdog.py REPLACES bot/_impl.py — net stays at 5. `.importlinter` `helpers-leaf` `forbidden_modules` extended with bot.orphan_db_watchdog; net contracts stays at 5. bot/_impl.py: 767 → ~582 LOC. **Sprint 9 main-class chunk CLOSES here.**
- Sprint 9 → Bit 9.3-iii.a SHIPPED 2026-05-11 — 4 boot-time bindings (`_HPSB_VALIDATOR_UNAVAILABLE_REASON`, `_HPSB_MISSING_BLEEDERS`, `_BLEED_BLOCK_MISSING_BLEEDERS`, `compute_for_15m_main_path`) + cal_mlp warmup boot log relocated to NEW `bot/boot.py` clean leaf (stdlib + bot.helpers.validators + scripts/cal_mlp/integration only; `import bot._thread_env` as defense-in-depth first non-stdlib import). bot/main_loop.py + bot/state.py top-import directly from bot.boot — Bit 9.3 method-body late-binding block + Bit 7.1 `_get_compute_for_15m_main_path()` helper RETIRED atomically. `.importlinter` `helpers-leaf` `forbidden_modules` extended with bot.boot; `state-no-impl-toplevel` contract RETIRED (3-layer anti-regression seal → stronger 1-layer "zero bot._impl edges" peer-pin); net contracts 7 → 6. bot/_impl.py: 582 → ~565 LOC.
- Sprint 9 → Bit 9.3-iii.b SHIPPED 2026-05-11 — `_BotProxy` ModuleType subclass in `bot/__init__.py` RETIRED entirely; `bot/__init__.py` is now docstring-only (38 LOC). `bot.X` reads no longer route through `bot._impl.X` — callers reach names via canonical submodules directly (`bot.constants.X`, `bot.main_loop.MainLoop`, `bot.state.StateManager`, etc.). Constants in `bot/config.py` (MAX_RISK_PER_TRADE, MARKET_BLEND_W, HOURLY_KELLY_FRACTION) reach via `bot.config.X` (Bit 12.1, 2026-05-12; pre-Bit-12.1 `config.X` against repo-root `config.py`). Bundled CALMLP-boot-log fu: `logging.basicConfig(force=True)` hoisted from inside `if __name__ == "__main__":` block in `bot/__main__.py` to module top-level (ordered after `import bot._thread_env`, before `from bot.main_loop import MainLoop`) so the `[CALMLP] enabled=N at boot` log emitted during `bot.boot` module-load reaches stderr at production runtime. ~110 files retargeted atomically (91+ test files + market_config.py + dashboard_snapshot.py + supabase_sync.py + analyst.py + scripts/audit/dump_public_api.py). `scripts/audit/dump_public_api.py::_probe_runtime_proxy_attrs` retired to a no-op (Layer 3 no longer applicable post-retirement). Net `.importlinter` contracts: 6 → 6 (proxy removal is opposite of carve-out). Sister sub-bit 9.3-iii.c (DELETE bot/_impl.py — MILESTONE) remains PENDING.
- Sprint 9 → Bit 9.3-iii.c **SHIPPED 2026-05-11** — DELETE bot/_impl.py + kill-switch runtime-freshness fix bundled. NEW `bot/runtime_config.py` PEP 562 dual-probe replaces the deleted bot._impl namespace for dashboard/supabase callers. 21 TDD pins green; 5,200 tests passing (no new regressions). .importlinter contracts 6 → 5 (engines-no-impl retired — forbidden_modules was empty). Sprint 9 main modularization CLOSED.
- Sprint 9 → Bit 9.4 (root CLAUDE.md sacred-rule semantics rewrite). Pending; shipped in parallel with 9.3-iii.c via the incremental CLAUDE.md edits in this Bit's atomic commit.
