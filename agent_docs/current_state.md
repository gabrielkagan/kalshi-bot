# Current State

> Update like a dashboard, not a doc. Re-state the date on every change.

**Last updated:** 2026-06-12 (Bit V.1 estimator-parity tape vol for the live-small overlays — first re-arm-gate Bit after the 2026-06-12 operator PAUSE)

## Live trading

- **OBSERVATION_MODE = False** — LIVE TRADING with real money
- **15M live assets:** BTC (88c+), ETH (75c+, 50-contract cap sub-80c), SOL (86c+, taker-first), XRP (92c+), HYPE (90c+, P2.3 2026-05-14), DOGE (85c+, P2.3 2026-05-14), BNB (90c+, P2.4 2026-05-19)
- **15M shadow assets:** ADA + BCH (T1 shadow 2026-05-30, branch `ada-bch-15m-shadow-t1`). `ADA_15M_SHADOW=True` / `BCH_15M_SHADOW=True`: bot subscribes Coinbase ADA-USD/BCH-USD + Kalshi KXADA15M/KXBCH15M, writes `filter_stage='ada_shadow'`/`'bch_shadow'` rows, submits ZERO live orders. NO per-asset MIN_ENTRY_PRICE/MAX_RISK/blend-weight constants (T3/T4 surface; never reached under shadow). At wiring time both Kalshi series had 0 markets minted (registered but cycle not started) → inert until Kalshi starts the cycle. ADA has no hourly series (KXADAD not live); BCH's KXBCHD exists but is NOT subscribed (15M-only). Both in `HOURLY_EXCLUDED_ASSETS` + `HOURLY_NO_EXCLUDED_ASSETS` as a safety belt. — Prior: BNB T4-promoted 2026-05-19 via P2.4 (T1→T1.5→T2→T3→T4 collapsed at T3 when Brier-sweep argmin converged w=0.20 in 2 days, n=721). Hourly HYPE/DOGE/BNB remain in `HOURLY_EXCLUDED_ASSETS` + `HOURLY_NO_EXCLUDED_ASSETS`. Onboarding history: HYPE/DOGE T1 shadow 2026-05-10 → T4 live 2026-05-14 via Brier-sweep raw_prob direct-promote (cal_mlp training arc retired); BNB T1 shadow 2026-05-17 → T4 live 2026-05-19 via same precedent.
- **XRP_15M_SHADOW = False** — XRP promoted to live at 92c+ (data: 41W/2L, 95.3% WR)
- **HYPE_15M_SHADOW = False / DOGE_15M_SHADOW = False** — P2.3 live promotion 2026-05-14. Per-asset constants wired (HYPE_MIN_ENTRY_PRICE=90, DOGE_MIN_ENTRY_PRICE=85, both MAX_RISK_PER_TRADE=0.10). MARKET_BLEND_W_BY_ASSET extended: HYPE 0.80, DOGE 0.60. NBBO_FALLBACK_GATES INTENTIONALLY omits HYPE/DOGE (orderbook-only first step). 14d Brier-monitored soak runs through 2026-05-28.
- **BNB_15M_SHADOW = False** — P2.4 live promotion 2026-05-19 (ticket 86b9zmj37, sibling to P2.3 86b9xv66a). Per-asset constants wired: BNB_MIN_ENTRY_PRICE=90 (per-tier WR n=272 100% at 90c+; 85-89c is +0.08c sub-fee EV), BNB_MAX_RISK_PER_TRADE=0.10 (conservative new-asset precedent), TM_ASSET_RISK_CAPS["BNB"]=0.10 (mechanical mirror), MARKET_BLEND_W_BY_ASSET["BNB"]=0.20 (B.1-equivalent Brier sweep argmin at n=721 — matches ETH pattern; raw model beats market by ~10% Brier), NBBO_FALLBACK_GATES["BNB"]=(90,99,300.0) (analog default mirror of ETH; post-T4 refinement follow-up when NBBO observations accumulate). Kill-switch clauses preserved at TM/WKND/OVN/DC eligibility sites — flipping BNB_15M_SHADOW=True reverts to shadow in lock-step. Hourly BNB stays in `HOURLY_EXCLUDED_ASSETS` + `HOURLY_NO_EXCLUDED_ASSETS` (15M-only promotion; matches HYPE/DOGE P2.3 post-T4 state). T1.5 external-feed bundle (ticket 86b9zmj15) SHIPPED 2026-05-17 — CROSS_EXCHANGE_SYMBOLS["BNB"]={binance,kraken,bybit} + COINGLASS_SYMBOLS["BNB"] + OKX poller FUNDING_SYMBOLS/OI_SYMBOLS + shadow_coverage_backfill OKX_FUNDING_INSTRUMENTS extended. Deribit BNB perp ABSENT (documented gap, mirrors HYPE). 14d Brier-monitored soak runs through 2026-06-02. **Regime caveat:** 100% WR at 90c+ in shadow window was directional-regime-conditioned (BNB pumped 2026-05-17 → 2026-05-19); rollback rule in plan doc fires if Brier exceeds baseline+5% on ≥3 consecutive days, net 14d PnL ≤ -$50, or single-day catastrophic loss ≥ -$25.
- **ADA_15M_SHADOW = True / BCH_15M_SHADOW = True** — T1 15M shadow onboarding 2026-05-30 (branch `ada-bch-15m-shadow-t1`, plan `kb/decisions/ada-bch-15m-shadow-t1-plan.md`). Registries wired: SERIES_TICKERS (KXADA15M/KXBCH15M), HOURLY_SERIES_TICKERS (KXADAD inert/KXBCHD), COINBASE_PRODUCTS (ADA-USD/BCH-USD), HOURLY_EXCLUDED_ASSETS + HOURLY_NO_EXCLUDED_ASSETS belt. Kill-switch clauses at TM/WKND/OVN/DC eligibility sites. state.py `ada_spot_at_decision`/`bch_spot_at_decision` columns wired end-to-end (closes the BNB-T1 silent-drop gap 86b9zn5pq proactively). NO per-asset live constants (T3/T4). T3 calibration sweep blocks on ~2-4wk settled corpus. Regression lock: `tests/integration/test_ada_bch_onboarding_t1.py`.
- **SOL_TAKER_FIRST = True** — bypasses maker, direct IOC at all STC
- **Decided contracts LIVE:** T1 (z≤-5), T1B (z≤-4, 95c+), T2 (z≤-3, 93-96c), T2-Z25 (z≤-2.5, 93-96c). All @ 20% fixed sizing. **T2-Z2 SHADOWED** (97a365f Apr 1, -$313 on 47 trades). Canonical: `kb/concepts/dc-strategy.md`
- **Overnight discount LIVE:** weekday 04-11 UTC, 89c+, STC≤600s, no DC overlap; sub-89c/STC>600s remain shadow
- **Weekend discount LIVE:** Sat/Sun, 90c+, STC≤600s, no DC overlap; sub-90c/STC>600s remain shadow
- **Loss burst cooldown LIVE (Apr 11):** per-asset 2h lockout after any 15M loss (+$441/30d counterfactual)
- **Weather NO-side KILLED 2026-05-16 (ce8e2d2):** lifetime n=167, 38.3% WR vs 70% assumed prior (Wilson 95% CI [23.6%, 47.0%]). Near-ATM zone (NO 39-40c ↔ YES 60-61c) is market-maker zone with no edge; `bracket_no_live` (far-ITM NO 4-12c, 91.7% WR n=157) is where NO edge actually lives. Now observation-only on both sides; re-research initiative live (ClickUp folder `90149436180`). Prior LIVE history (Apr 11 → May 16): NO 39-40c, STC ≥ 16h, 1-contract, floor tightened 36→37 May 1 then 37→39 May 2 as sub-bands underperformed.
- **STC zones:** scan 0-900s, core live 0-300s, extended live 300-600s (per-asset higher floors), shadow 600-900s
- **STC sizing scaler:** contracts *= 300/STC for 15M at STC>300s
- **SOL sub-86c gate:** blocks SOL ≤85c at STC≥300s
- **LPNE LIVE:** BTC 80-87c near-expiry (STC 10-120s), 50ct fixed, prob ≥ price/100
- **P4.1 band-calibrated sizing LIVE (2026-05-17, ClickUp 86b9zjrp7):** 15M `_sizer.compute()` now receives `calibrated_prob_for_sizing(asset, best_ask, final_prob, product_type)` instead of bare `final_prob`. Helper home: `bot/helpers/band_calibration.py`. 42-cell empirical lookup (6 assets × 7 bands: 70-79 / 80-85 / 86-89 / 90-93 / 94-96 / 97-98 / 99) with hierarchical shrinkage k=30 toward band-aggregate prior. Hybrid lookback: 30d for bands 70-93c (regime-sensitive), 60d for bands 94-100c (thin-cell stability). Baseline frozen 2026-05-17 09:35 UTC against VPS HEAD `e3aecd4`. **Trade-selection gates unchanged** — only Kelly magnitude changes. **V2 sizing (`_v2_prob`) and NO-side (`no_prob`) explicitly out of scope.** Hourly/SPX/weather pass-through. 14d band-stratified Brier soak runs through 2026-05-31; rollback rule = per-(asset×band) realized rate ±5pp of baseline. Baseline sidecar: `agent_docs/p4_1_calibration_baseline.md`.

- **Vol engine Bit V.2 (2026-06-12): beta×DVOL path KILLED.** `bot/engines/volatility.py::_get_implied_vol`/`_get_implied_vol_hourly` return `None` for every asset outside `DERIBIT_DVOL_CURRENCIES` ({BTC, ETH}) — previously `btc_dvol × _estimate_beta(asset)` deflated alt `blended_rv` up to ~6x (cross-asset-identical ~8.5e-5 cluster vs ~3e-4 tape; chronic since the DVOL integration 2026-02-23, caught in the 2026-06-12 live-small day-one autopsy). Alts now take the rv/EGARCH fallback. Same Bit fixed the IV inverse-variance blend to use the EGARCH-promoted `blended` instead of `rv_blended` (the promoted EGARCH layer was silently discarded whenever IV fired). `_estimate_beta` is DEAD CODE pending a removal follow-up. Regression lock `tests/integration/test_vol_engine_beta_dvol_regression.py`. Sister Bits: V.1 (tape_rv300 gate re-arm), V.3 (persist + honesty alert). **Bit V.4 (2026-06-12, same day): IV blend REMOVED entirely — DVOL is diagnostic-only.** The V.3 monitor caught the surviving BTC/ETH IV path overstating vol 1.8-2.7x through the first soak evening (stress_override fired on 60-91% of ticks: a level-spread trigger that VRP + the diurnal trough satisfy every quiet evening; day-1 soak medians BTC 1.262 / ETH 1.386 vs the [0.8,1.25] band). 9-day journal counterfactuals measured both IV branches QLIKE-negative (the inverse-variance branch dragged blended to ~0.55× truth on the Jun-5 spike day; the one IV-positive segment is matched by EGARCH alone). BTC/ETH now take the same rv/EGARCH path as alts; `dvol_5s`/`iv_rv_spread`/`vrp` diagnostics stay live for the W.1/W.2 replacement program (seasonality × log-HAR, tickets 86bae6t6r + 86badv7xj); `IV_RV_SPREAD_THRESHOLD` deleted. Regression lock `tests/integration/test_vol_engine_iv_diagnostic_only_regression.py`. Restarts the live-small re-arm soak clock on deploy.

## Disabled / observation

- **Live-small PAUSED 2026-06-12 (operator) — re-arm gated on the vol-honesty Bit chain (V.1 ✅ / V.2 ✅ shipped 2026-06-12 PR #165 / V.3).** Both engines stay ENABLED in shadow (`LONGSHOT_ENABLED=True` + `TWAPLOCK_ENABLED=True`, `LONGSHOT_LIVE_OVERRIDE=False` since ~12:05Z + `TWAPLOCK_LIVE_OVERRIDE=False` since 12:57Z (PR #164 merge `040fe826`) — free would-be-fill measurement). RCA: `blended_rv` for non-BTC/ETH assets is, in most regimes, `clamp(beta, 0.5, 3.0) × BTC_DVOL` — 1.4-4x understated vs the tape; 10/11 day-one longshot fills failed the validated entry rule under honest vol (-$8.10/4 losses in 11 windows); twaplock's `p_lock` consumes the SAME input (postmortem `kb/failures/vol-engine-beta-dvol-deflation-jun12.md`). **Bit V.1 SHIPPED 2026-06-12 (this commit, gates re-arm):** `bot/helpers/tape_rv.py::trailing_rv300` — exact formula parity with the validated backtests' trailing-300s tape RV (`02_longshot_tick_floor._rv` / `fairvalue_extract._realized_vol`; 5s grid, 30s per-grid-point staleness, sample stdev /(n-1)) — computed once per asset per scan tick from the CoinbaseFeed 1s buffer, staged (post V.3-R1-M1) as the tape half of the ATOMIC `StateManager._scan_vol_pair_cache[asset] = (raw_blended_rv, tape_rv300)` pair, and BOTH overlays now receive `_strategy_vol = max(blended_rv, rv300)` (conservative — never price short-vol/lock risk off the smaller estimate; rv300 None → blended fallback + throttled `TAPE_RV_NONE` at INFO). **Staleness is TWO-LAYER (R1-M1 fix round):** the helper's per-grid-point guard operates on BUFFER time and covers buffer-shape gaps only (warmup/short buffer/stalled sampler) — the live CoinbaseFeed sampler re-stamps the last-known price with fresh timestamps every 1s, so a frozen WS feed presents a gapless flat buffer on which rv300 reads LOW (~0), never None. The scanner seam therefore ALSO gates the cache write on the Bit-S.1 EVENT-time staleness reading (`_scan_spot_staleness_cache`): missing or > `TAPE_RV_MAX_STALENESS_S` (30.0 = the backtest's abstention horizon, 02's `STALE_S`) ⇒ rv300 treated None (pop + fallback). The same fix round added the missing **longshot engine-level staleness gate** — `LONGSHOT_MAX_SPOT_STALENESS_SECONDS=30.0`, the twaplock `TWAPLOCK_SPOT_STALE` pattern (twaplock's own 5s gate predated this; longshot had NONE): event-stale/unmeasured spot ⇒ no candidates, no eval rows, throttled `LONGSHOT_SPOT_STALE` info log — on frozen feeds the overlays abstain from ENTRY like the backtest did rather than price off the deflation-prone blended_rv — **plus quote-down (R2-M1 fix round):** a stale episode persisting past `LONGSHOT_STALE_CANCEL_GRACE_SECONDS=10.0` (= 02b's measured `LATENCY_S` cancel-latency arm, pickoff -0.16c/ct; grace absorbs flickery staleness, fresh eval clears the per-ticker episode clock) cancels that ticker's resting longshot quotes with reason `spot_stale` — entry abstention alone left resting quotes up un-refreshed through ≤12-min stale episodes whose fills the 02b fill model (zscore→None on a >30s-stale spot at print time) had EXCLUDED from the +4.58c economics. Twaplock is taker/IOC (rests nothing), so entry abstention is the complete fix on that side. Estimator parity is L-VOL-1, AST-pinned (`tests/contracts/test_tape_rv_estimator_parity.py`). **Remaining before re-arm:** Bit V.3 (V.2 shipped to main 2026-06-12, PR #165 `cb8ddf18` — see the Vol engine Bit V.2 bullet) (persist the RAW vol pair on evaluated_opportunities + throttled honesty alert + /data-health; gate = 7d shadow soak with per-asset median raw_blended_rv/rv300 ∈ [0.8, 1.25] + 02b replay with live-engine vol matching backtest selectivity). **Ratio data source (R1-M2, atomic-pair form post V.3-R1-M1):** the soak ratio's numerator (RAW engine estimate) and denominator (tape rv300) are the two halves of the single `StateManager._scan_vol_pair_cache[asset]` tuple — the same-tick VOL-ENGINE pair V.3 persists at eval-row write time (written/popped at the 15M seam only; persist-both-or-neither; auto-fill gated to 15M product_type so cooldown/hourly paths can't leak stale pairs). It must NOT be computed from the rows' existing `volatility` column: the overlays persist their vol kwarg, which post-V.1 is `max(blended_rv, rv300)` (correct decision provenance), so a row-sourced ratio is ≥ 1 by construction and can never detect deflation.
- **Longshot premium-harvest (Bit L-1, 2026-06-11): default OFF.** `LONGSHOT_ENABLED=False` — flipped only at explicit operator go-live. Engine `bot/longshot.py`: sells deep-OTM 15M sides as a maker (posts opposite-side bid at 100−ask) when the executable ask is 4-15¢, STC 180-720s, and `p_normal ≤ ask × 0.5`; holds to settlement. Validated +4.58¢/ct fillable-only, day-bootstrap CI [+2.82, +6.26], 12/12 days, all 6 assets positive (`scripts/research/genhunt/02b_longshot_fillable_validation.py`). Rails: 3 contracts per (window, side); $150 concurrent collateral; $20/day COMBINED live-small loss cap shared with twaplock via `bot/strategy_caps.py` (`LIVE_SMALL_DAILY_LOSS_CAP_DOLLARS`; realized + marked — open positions whose sold side is currently ITM count as full loss); 3 consecutive COMBINED losing days → persistent disable (`LIVE_SMALL_STREAK_RESET_UTC_DATE` to clear; the Bit L-1 per-strategy `LONGSHOT_*` cap/streak constants are RETIRED — see the Bit T-1 note at the end of this bullet). Go-live path: `LONGSHOT_ENABLED=True` + `LONGSHOT_LIVE_OVERRIDE=True` runs longshot live while the main pipeline stays shadow (`trading_mode.strategy_is_live`; main pipeline unaffected) — asset-scoped (R4-M1) to `LONGSHOT_LIVE_ASSETS` = BTC/ETH/SOL/XRP/HYPE/DOGE/BNB (the whole strategy branch, override leg AND any future dual-live flip): the 6 02b-validated assets + BNB per the 2026-06-12 operator directive (02b absence = corpus artifact, no replayable spot source; live engine has BNB spot via bot feeds); ADA/BCH excluded — Kalshi 15M series not yet listed (zero corpus windows) + T1 zero-live-orders shadow designation. Orders carry the `ls-` client_oid prefix (boot orphan reconciliation + backstop recognition). R1 fix round 2026-06-11 hardened: fills polled before every cancel, ticker-PK collision stopgap (86badbf9t carries the durable rebuild), 404-terminal cancels + stale drop, one paginated fills fetch per tick. R2 fix round 2026-06-11 hardened: longshot rows excluded from the scanner's timeslot-occupancy filter + every quote pop marks the pending_orders row canceled/filled (pre-fix a single placement starved the engine AND main-pipeline evaluation of that window from the next tick — scan()-level regression harness at `tests/integration/test_longshot_r2_regressions.py`); boot reconciliation bounds fill fetches by the order's created_time and also reconciles ls- orders no longer resting at boot (fully filled/expired pre-restart); FP-primary field reads (`count_fp`/`fill_count_fp`/`remaining_count_fp`); zero-parse fills retried instead of trade_id-blacklisted; malformed place responses (no order_id) never register a quote (`LONGSHOT_PLACE_MALFORMED`). R3 fix round 2026-06-11 hardened the restart path: startup `reconcile_with_api` SKIPS `ls-` orders (no cancel, no row flip — `RECONCILE_SKIP_LONGSHOT`) and the engine's first-tick boot step then adopts/reconciles them; unknown positions import as `strategy_group='longshot'` (`RECONCILE_IMPORT_LONGSHOT`) when the ticker's MOST RECENT pending order is `ls-`-prefixed (R4-MN2 recency) so they stay inside the caps/marks/streak rails; boot fill application is reconcile-aware delta-apply (only `max(0, fetched − skip)` contracts recorded, oldest first — no double-count on restart); partial fills snapshots (mid-pagination failure / page-cap exhaustion) record what they fetched but never terminally mark rows, and a 404-after-fill-mismatch cancel defers its pop until a clean (complete) fills poll on a later tick (`LONGSHOT_CANCEL_GONE_DEFERRED`). Regression lock `tests/integration/test_longshot_r3_regressions.py`. R4 fix round 2026-06-11 hardened: ONE open longshot row per ticker — `_allowed_size` zeroes (and the scanner overlay defensively filters) any quote whose buy side opposes an open longshot row (`LONGSHOT_SKIP_side_conflict`; pre-fix an opposite-side fill accumulated under the old side and settled inverted — 86badbf9t carries the durable composite-PK rebuild); boot delta-apply seeds per-order via NEW `pending_orders.recorded_fill_count` (bumped by `_apply_fills` on each successful record; explicit 0 on fresh rows, NULL legacy rows fall back to the (ticker, side) aggregate — the aggregate misattributed across sequential same-(ticker, side) orders and dropped a real later-order fill); `cleanup_expired_resting_orders` skips `ls-` rows (settlement daemon runs it concurrently; an 'expired' flip hid rows from boot step 2); boot step-1 adoption registers cumulative-unit count (remaining + skip) so a partially-prefilled orphan ends 'canceled' with truthful (filled/count) logs. Regression lock `tests/integration/test_longshot_r4_regressions.py`. R5 fix round 2026-06-11 hardened: a failed `record_position_from_fill` un-stamps the trade_id + restores the consumed skip budget so the next poll retries cleanly (pre-fix a transient `database is locked` permanently blacklisted the fill — quote popped 'canceled', position invisible to all rails; bounded double-record accepted in the committed-despite-raise race, fail-toward-recording); the one-open-longshot-row-per-ticker invariant extends to opposite-side RESTING quotes incl. CANCEL_FILL_MISMATCH-held entries (`has_opposite_side_resting_quote` — registry quotes are future rows; checked in `_allowed_size` under the same lock hold as the sizing scan + mirrored at the scanner overlay); boot step-1 adoption seeds the REAL `seconds_to_close` from the ticker's close epoch (pre-fix 0.0 made the stale-drop backstop fire 120s after RESTART, abandoning a live order's entry; unparseable ticker falls back to 0.0); the stale-drop pops only after a COMPLETE final fill poll (failed/partial polls defer one tick — `LONGSHOT_STALE_DROP_DEFERRED`); absent remaining-count fields derive remaining = max(0, original − skip) so a fully-recorded orphan ends 'filled', not 'canceled'; boot step-2 queries `status IN ('resting','pending')` (crash-between-place-and-confirm rows were invisible forever) and step-1 repairs API-present pending rows via `confirm_order_submitted` so lifecycle marks land. Regression lock `tests/integration/test_longshot_r5_regressions.py`. R7 fix round 2026-06-11 hardened: boot step-1 adoption extracts the order price DOLLARS-FIRST (`no_price_dollars`/`yes_price_dollars` via `dollars_str_to_cents`, legacy integer fields as fallback — post-FP-transition /orders objects carry only the dollar fields, so the pre-fix legacy-only read adopted orphans at `buy_price_cents=0`: recovered fills recorded a ZERO cost basis and the daily-cap/streak rails were blind to exactly the boot-path losses), plus a `count_fp`-primary twin on the original-size derive. Regression lock `tests/integration/test_longshot_r7_regressions.py`. **Bit T-1 (2026-06-11) retargeted the daily-cap/streak rails to the COMBINED live-small helper** — `LongshotEngine._refresh_disabled` now consumes `bot/strategy_caps.py` (realized+marked across `strategy IN ('longshot','twaplock')` vs `LIVE_SMALL_DAILY_LOSS_CAP_DOLLARS`=20 + `LIVE_SMALL_CONSECUTIVE_LOSING_DAYS_DISABLE`=3; `LIVE_SMALL_STREAK_RESET_UTC_DATE` to clear); the per-strategy `LONGSHOT_DAILY_LOSS_CAP_DOLLARS`/`LONGSHOT_CONSECUTIVE_LOSING_DAYS_DISABLE`/`LONGSHOT_STREAK_RESET_UTC_DATE` constants are RETIRED. The `ls-` reconciler carve-outs in `bot/state.py` generalized to `ENGINE_OWNED_CLIENT_OID_PREFIXES` (ls-/tw-); log signatures keep per-strategy suffixes.
- **TWAP-lock endgame taker (Bit T-1, 2026-06-11): default OFF.** `TWAPLOCK_ENABLED=False` — flipped only at explicit operator go-live. Engine `bot/twaplock.py`: in the [60, 90]s-to-close window (`TWAPLOCK_ENTRY_WINDOW_SECONDS`=90 upper edge = the validation's DEC_FROM; engine-side `_MIN_SUBMIT_STC_SECONDS` RAISED 10→60 on 2026-06-14 — the <60s window was measured locking only 0.754 vs a predicted 0.99 in the post-V.4 shadow soak, a TWAP variance-collapse + omitted Coinbase-vs-RTI basis-error bug, NOT vol deflation, honest vol would need to be 4× larger; the retained [60, 90]s window locks 0.990 and is vol-calibrated; `kb/decisions/twaplock-stc60-floor-plan.md`, reopening <60s needs a basis-error variance model), computes `p_lock` for the TWAP-settled outcome from the accrued Coinbase-anchored settlement-TWAP (per-asset (ts, price) ring buffer fed from the scanner's per-tick spot read; time-weighted step-hold mean over the elapsed portion of the final-60s window) plus a Brownian remaining-variance term from `blended_rv` (with the 60s floor the live path prices off current spot + the Brownian term — the accrued-mean branch is dormant, retained for a future <60s reopening); when the locked side clears `TWAPLOCK_P_LOCK_THRESHOLD=0.99` (stricter than the validated 0.95 — Coinbase-vs-RTI basis adds proxy error; undercounting costs frequency, not correctness), BUYs that side as a TAKER (IOC via `OrderExecutor._execute_twaplock_taker`) iff the executable ask ≤ 100 − taker_fee(1ct) − `TWAPLOCK_MIN_EDGE_CENTS`=3; holds to settlement. Validated +14.4¢/ct, day-bootstrap CI [+11.1, +17.7], n=359 over 12 days, 29.9 locks/day on the honest 4-venue index, print cross-check 99.2%, all 7 assets positive (`scripts/research/genhunt/01b_twap_lock_validation.py`). Rails: max 2 ct/entry; ONE entry per window per asset (STRUCTURAL, no knob — binary in-memory latch + ANY tw- pending_orders row on the ticker, even a zero-fill canceled IOC — survives restart; the former `TWAPLOCK_MAX_ENTRIES_PER_WINDOW` constant was RETIRED at R1-MN4); cross-strategy ticker exclusion (never enters a ticker with ANY open position or pending/resting order from ANY strategy — single-ticker positions PK until 86badbf9t; fail-closed); COMBINED $20/day cap + 3-consecutive-losing-days disable shared with longshot via `bot/strategy_caps.py`; frozen-spot false-lock gate (R2-MN1 — NO SIGNAL when the scanner's per-asset Bit-S.1 staleness reading `state._scan_spot_staleness_cache` is missing or > `TWAPLOCK_MAX_SPOT_STALENESS_SECONDS`=5, log `TWAPLOCK_SPOT_STALE`; a frozen WS price keeps the ring buffer ticking with fresh timestamps, so the absent-sample layer alone cannot catch a stale-but-ticking feed). NO resting-quote lifecycle: an IOC never rests — no registry, no cancel sweeps; the first-tick boot sweep flips stranded tw- pending/resting rows to 'canceled' (positions-API reconcile owns any hidden fill, importing as `strategy_group='twaplock'` via the recency-stamped tw- pending history). Fill read is FP-primary from day 1 (`fill_count_fp` via `fp_str_to_int`, legacy fallback); a malformed count field degrades to the 0-fill path (`TWAPLOCK_FILL_PARSE_MALFORMED` — row canceled, no position, shot consumed; R1-MN5) and a no-order_id response marks api_error (`TWAPLOCK_PLACE_MALFORMED`); positions recorded at the limit price (conservative; startup reconcile corrects). filter_stage literals `twaplock_live`/`twaplock_shadow`. Scanner overlay sits after the longshot overlay in the market loop; candidates partition out at the scan() tail and route through `executor.execute()` — the trading-mode gate stays the single live/shadow chokepoint (`TWAPLOCK_LIVE_OVERRIDE=False` mirrors longshot's single-strategy go-live). INTENTIONAL occupancy asymmetry vs longshot R2-C1: twaplock rows DO occupy their timeslot slot (blocks main-pipeline entry into that window's final ~90s — cheap reverse-direction ticker-PK defense). Go-live path: `TWAPLOCK_ENABLED=True` + `TWAPLOCK_LIVE_OVERRIDE=True` — asset-scoped (R4-M1) to `TWAPLOCK_LIVE_ASSETS` = the 7 validated assets BTC/ETH/SOL/XRP/HYPE/DOGE/BNB (mirrors the 01b `TRACKED` tuple; the whole strategy branch, override leg AND any future dual-live flip): ADA/BCH are T1 zero-live-orders shadow (`ADA_15M_SHADOW`/`BCH_15M_SHADOW`) + not in the 01b corpus — excluded on both grounds. Regression lock `tests/integration/test_twaplock_strategy.py` (suite incl. the executor-chokepoint truth table; tests incl. executor-chokepoint truth table through the REAL OrderExecutor).
- **Hourly FULLY DISABLED (Apr 18):** Both `HOURLY_LIVE_ENABLED=0` and `HOURLY_NO_SIDE_LIVE=0` on VPS. Pre-kill NO-side: BTC NO 40-54c had 53.9% WR (n=1,113, p=0.005). Re-enable: flip env vars + restart.
- **SPX Hourly:** observation (was briefly live Mar 17, reverted — Polygon 403 broke vol engine)
- **Weather YES-side:** observation only (NWP ensemble GFS+ECMWF, 82 members, 19 cities)
- **Sports:** observation only (basketball best, 69.2% WR n=39, SPRT CONTINUE_COLLECTING). Apr 12 fix: 31-day data outage from MLB code mismatch + LA→LAK + hockey re-enabled for playoffs.

## Shadow strategies

- **15M:** A1 (RecalibratedEGARCH), A2 (LightGBM), A3 (EGARCH gating), A4 (LateWindow 55-74c) — `fifteenm_shadow.py`
- **DC variants:** dc_shadow_t1b_93c, dc_shadow_t2_90c, dc_shadow_t2_90c_xrp, dc_shadow_no_side
- **Calibration:** Per-city weather + per-sport-group CalEngines learning in shadow
- **Hourly NO 40-54c:** 1-contract verification mode (env var kill switch)
- **Low-price 70-79c:** dual-sizing sim (full Kelly vs LP_KELLY=0.25, LP_MAX_RISK=0.10)
- **Position price monitor:** logs yes_ask/bid for held 15M positions via WS
- **Multi-venue synthetic RTI (B2b-1, ticket 86ba64h2w):** in-bot 4-venue (Coinbase/Kraken/Bitstamp/Gemini) L2 → CFB-shape synthetic, logged to `evaluated_opportunities.rti_synthetic/rti_constituent_count/rti_confidence`. **SHADOW by default — feeds a decision only for assets in `SYNTHETIC_RTI_LIVE_ASSETS` (RTI-6 per-asset go-live gate; default EMPTY ⇒ shadow for every asset).** Kill-switch `SYNTHETIC_RTI_ENABLED` (env, default OFF): when OFF the feed (`bot/feeds/synthetic_rti_feed.py`) opens no sockets/threads. Computed off the scan hot path by a sampler daemon; the scan loop reads an O(1) cache. NEVER flip the signal live before the Bit-4 validation gate (`kb/decisions/b2b-multi-venue-signal-program-plan.md`).

## Tests

3,368 tests across 16+ test files (Apr 26: +28 for 15M silence watchdog hardening).

## Tech stack

- Python 3, virtualenv
- DigitalOcean droplet (45.55.181.30), Ubuntu 24.04, `botuser`, systemd `kalshi-bot`
- Dashboard: Supabase Realtime (`dashboard_state` table)
- Analyst: Claude API via `bot/ai/analyst.py` → Telegram (high-confidence only)

## Kalshi API

- **Auth:** RSA-PSS signature with `/trade-api/v2` prefix
- **Orderbook:** YES and NO are SEPARATE; YES + NO prices do NOT always sum to 100
- **All orders are limit orders** (no market orders)
- **Tier:** Advanced (30 reads/sec, 30 writes/sec)
- **Series (15M):** KXBTC15M, KXETH15M, KXSOL15M, KXXRP15M, KXHYPE15M (live, P2.3 2026-05-14), KXDOGE15M (live, P2.3 2026-05-14), KXBNB15M (live, P2.4 2026-05-19, 86b9zmj37), KXADA15M (shadow, T1 2026-05-30), KXBCH15M (shadow, T1 2026-05-30)
- **Series (hourly):** KXBTCD, KXETHD, KXSOLD, KXXRPD, KXHYPED (excluded), KXDOGED (excluded), KXBNBD (excluded — 15M-only promotion at P2.4), KXADAD (not live on Kalshi; inert registry entry), KXBCHD (excluded — not subscribed; ADA/BCH 15M-shadow-only T1)
- **Series (weather, 19 cities):** KXHIGHNY, KXHIGHCHI, KXHIGHMIA, KXHIGHDEN, KXHIGHLAX, KXHIGHAUS, KXHIGHTATL, KXHIGHTSFO, KXHIGHTDAL, KXHIGHTPHX, KXHIGHPHIL, KXHIGHTMIN, KXHIGHTSEA, KXHIGHTHOU, KXHIGHTBOS, KXHIGHTLV, KXHIGHTOKC, KXHIGHTDC, KXHIGHTNOLA

## Fees

- **Taker:** `ceil(0.07 * C * P * (1-P))` — ~1% of edge at typical prices
- **Maker:** $0

## Order execution

- **Default:** maker-first (post_only=True) → escalates to taker if unfilled
- **SOL exception:** taker-first (`SOL_TAKER_FIRST=True`)
- **Decided contracts:** route direct taker
- **Taker allowed at all STC** (`MAKER_ONLY_THRESHOLD=0`)
- **Escalation:** maker → poll queue → cancel-replace IOC taker
- **Fill detection:** WS primary, REST fallback
- **Candidate logging:** observation_trade (obs mode) + candidate (live mode) → `evaluated_opportunities`
