# silver/ ETL stack (D3.0 → D3.0-fu1, 2026-05-18).
#
# Pinned per the plan-doc Bit-kickoff verify list. Compatibility matrix:
#   https://docs.getdbt.com/reference/warehouse-setups/duckdb-setup
#
# Update protocol: when bumping any of these, re-run the integration
# tests (tests/integration/test_silver_etl_pipeline.py) end-to-end
# against the new version before committing.
#
# D3.0-fu1 (this revision) uncomments the dbt-core + dbt-duckdb pins
# that D3.0 deferred — the silver ETL now subprocesses `dbt run` against
# the silver/ dbt project. See `silver/scripts/etl_run.py` for the
# wrapper and `silver/dbt_project.yml` for the project definition.

# Core ETL stack
duckdb==1.1.3
dbt-core==1.8.7
dbt-duckdb==1.8.4

# Bronze chunk decompression (already in root requirements.txt at D1.2
# for collector — listed here for silver/ standalone install on the
# operator's Mac, NOT a re-add)
zstandard>=0.22.0
