# silver/ — D3.0 ETL stack (ticket 86b9zxc6t, 2026-05-18).
#
# Per the plan-doc Bit-kickoff verify list item #1 (decision #6): pin
# exact versions. The compatibility matrix is at:
#   https://docs.getdbt.com/reference/warehouse-setups/duckdb-setup
#
# Update protocol: when bumping any of these, re-run the integration
# tests (tests/integration/test_silver_etl_pipeline.py) end-to-end
# against the new version before committing.

# Core ETL stack (D3.0 ship — direct-DuckDB only)
duckdb==1.1.3

# Bronze chunk decompression (already in root requirements.txt at D1.2
# for collector — listed here for silver/ standalone install on the
# operator's Mac, NOT a re-add)
zstandard>=0.22.0

# DEFERRED to D3.0-fu1 (dbt-project layering) — NOT installed at D3.0:
#     dbt-core==1.8.7
#     dbt-duckdb==1.8.4
# D3.0 ships direct-DuckDB ETL (path c "custom python materialization");
# the dbt-core + dbt-duckdb pins land in this file when D3.0-fu1
# (URGENT, see README "Followups") refactors `silver/scripts/etl_run.py`
# into a dbt-run subprocess wrapper. Listing them here pre-ship would
# bloat operator's `pip install` with unused deps.
