{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty') && if echo \"$FILE\" | grep -q '\\.py$'; then python3 -c \"import ast; ast.parse(open('$FILE').read())\" 2>&1 || echo \"SYNTAX ERROR in $FILE\"; fi"
          }
        ]
      }
    ]
  }
}
